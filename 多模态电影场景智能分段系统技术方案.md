# 一种基于多模态影视理解与群体脑电反馈的电影智能分段、价值评估及自动剪辑系统

> 文档类型：技术交底书 / 项目技术方案（Markdown 重写稿）  
> 编写依据：Movie Analysis Platform 项目的 `README.md`、`IO_Rule.md`、`FASTAPI_MULTIMODEL_REFACTOR_PLAN.md`、`DOUBAO_ASR_NGROK_CHAIN_FIX_PLAN.md`、统一 Schema、模型 Adapter、Workflow/Executor、Artifact 与媒体处理代码，以及 movie_EEG 项目的 `README.md`、`eeg_core` 共享算法包、Edit 剪辑策略、CLI、PySide6 GUI 与 LLM Agent 代码。
> 说明：本文沿用参考文档“背景技术—发明内容—技术方案—实施方式—创新点—权利要求概述—技术效果”的撰写思路。硬件部分保留参考文档原有内容；软件、模型、脑电算法、数据流和系统实现已按两个项目的融合逻辑重写。本文区分“已实现/已验证”“已有代码但待正式收口”“规划中”三种成熟度，不把计划指标表述为实测结果。

---

## 一、基本信息

| 项目 | 内容 |
|---|---|
| 发明名称 | 一种基于多模态影视理解与群体脑电反馈的电影智能分段、价值评估及自动剪辑系统及其处理方法 |
| 技术领域 | 人工智能视频理解、电影镜头检测、场景边界识别、多模态语义分析、视觉语言模型、前额脑电信号处理、群体神经同步分析、人机协同剪辑、分布式任务编排、数字媒体处理 |
| 核心技术问题 | 如何先依据影视内容形成叙事完整、边界可解释的 Scene，再将 Scene 时间轴与多被试前额 EEG 对齐，通过 Attraction、Engagement、Fatigue、Workload 与 ISC 五类指标判断各片段的群体观看价值，并在保留叙事结构的条件下生成可追溯剪辑建议和剪辑后视频 |
| 系统边界 | 输入为原始电影视频或其存储 URI，以及与播放时间轴同步的单被试或多被试 BDF/EDF 脑电数据；中间输出包括 Video、Shot、Subtitle、Scene、SceneEvidence、`scene_score`、EEG 窗口指标与剪辑建议；最终输出为建议时间段、保留/删除决策记录、剪辑工程数据和剪辑完成后的视频 |
| 当前成熟度 | 视频侧已形成 FFmpeg 标准化、OmniShotCut 闭环、关键帧、Schema、Artifact/Manifest、Celery 骨架及部分 VLM/ASR 原型；EEG 侧已形成 BDF/EDF 加载、预处理、四指标评分、群体 ISC、剪辑建议、CLI/GUI/Agent 和 CSV/JSON 导出代码；SceneSeg/BaSSL、视频侧最终装配及“剪辑建议→FFmpeg 成片”的跨项目执行接口仍需完成与验证 |

### 1.1 背景技术

长电影、纪录片、课程录像和影视素材通常由大量镜头组成。镜头（Shot）是一次连续拍摄形成的最小视觉单元，而场景（Scene）是由若干时间连续、人物关系相对稳定、空间语义一致并共同服务于一个叙事事件的镜头集合。镜头切换不等于场景切换：对话中的正反打、同一空间内的机位变化、特写与全景交替都会产生镜头边界，但并不应切成新场景。

现有视频自动分段方式主要存在以下问题：

1. **只检测像素突变，无法理解叙事连续性。** FFmpeg scene filter、直方图差异等传统方法擅长发现硬切，却难以判断“同一场所换机位”和“真正换场”之间的区别。
2. **把 Shot Boundary 误当作 Scene Boundary。** 一部电影可能包含上千个镜头；若每次镜头切换都分段，会产生过度切分，破坏对白、动作和叙事事件的完整性。
3. **单一视觉模型信息不足。** 黑场、闪白、快速运动、蒙太奇、相似布景和交叉剪辑都可能造成视觉误判；仅靠字幕又无法处理无对白段落。
4. **时间基准不统一。** 浮点秒、帧号、PTS 与字幕时间若混用，会在长视频中积累漂移，导致关键帧、字幕和模型输出无法精确对齐。
5. **模型输出缺少工程约束。** 若模型直接读写数据库、直接决定最终切点或把大型 Tensor 放入消息队列，会造成不可复现、难重跑、难扩展和故障传播。
6. **结果缺少证据链。** 只保存最终分段时间戳，无法回答某个边界为何被选中，也无法在模型升级后进行回归比较。
7. **长视频成本与吞吐压力大。** 对整部电影按固定 0.2 秒密集采样会生成数万候选点；将全部画面送入 VLM 不仅成本高，而且最终仍需吸附回自然镜头边界。
8. **只按内容分段，无法回答片段是否真正吸引观众。** 视觉和字幕模型能够识别场景变化，却不能直接测量观众在观看过程中的投入、趋近、疲劳、认知负荷与群体同步反应。
9. **脑电评分与电影结构彼此割裂。** 若只按固定 EEG 窗口直接裁切，建议区间可能落在一句对白、一个动作或一个 Scene 的内部，造成语义残缺和剪辑跳变。
10. **从分析到成片缺少闭环。** 许多系统止步于曲线或时间戳，缺少“Scene 结构约束—EEG 价值判断—人工复核—物理裁切与拼接—结果追溯”的完整执行链。

因此，本项目要解决的不是“再增加一个切镜模型”或“只绘制一组脑电曲线”，而是建立一套从视频规范化、自然镜头检测、多模态 Scene 构建、EEG 同步分析、片段价值判断、约束剪辑到成片装配的完整系统。

### 1.2 现有方法与本方案对比

| 对比项 | 常见方法 | 本方案 |
|---|---|---|
| 候选点来源 | 固定时间间隔或像素突变 | 先由 OmniShotCut/FFmpeg 形成自然 Shot 边界候选集 |
| 场景判断 | 单一视觉阈值 | 视觉、人物、场所、字幕、音频与时间间隔的多模态证据融合 |
| 模型权限 | 模型直接输出切点 | 模型只产生候选证据；最终切点由编排层的选择算法确定 |
| 时间规则 | 浮点秒、帧号混用 | 内部统一整数毫秒，区间统一为 `[start_ms, end_ms)`，FPS 使用 `fps_num/fps_den` |
| 输出评分 | 多套含义交叉的分值 | 对外仅保留归一化到 `[0,1]` 的 `scene_score` |
| 长视频处理 | 密集抽帧、重复解码 | 每个 Shot 抽取 25% 与 75% 代表帧，单次顺序解码并全局去重 |
| 中间结果 | 常被覆盖或不保存 | 每一步形成 Artifact 与 `.manifest.json`，包含模型、版本、哈希和运行信息 |
| 执行方式 | 同步请求或进程内后台任务 | FastAPI 仅创建任务；Celery 按队列调度耗时处理 |
| 扩展方式 | 业务代码直接调用模型 SDK | 所有模型通过统一 Adapter 和 I/O Contract 接入 |
| 片段价值依据 | 编辑者主观判断或播放量等事后指标 | Scene 内 Att、Eng、Fat、WL 与多被试 ISC 的时间对齐证据 |
| EEG 分析 | 单被试均值或整场平均 | 12 秒滑窗、质量门控、组平均曲线与 leave-one-out ISC |
| 删除规则 | 单指标固定阈值 | `低投入` 或 `低趋近+高负荷+高疲劳`，再与极端低 ISC 取并集 |
| 剪辑粒度 | 直接按 EEG 窗口截断 | EEG 窗口先映射至 Scene/Shot 合法边界，保护叙事完整性 |
| 最终产物 | 时间戳或报告 | 剪辑建议、证据与参数、EDL/JSON/CSV 以及剪辑完成后的视频 |

---

## 二、发明内容

### 2.1 发明目的

本方案旨在提供一种面向电影和长视频的多模态智能分段系统，使其能够：

1. 将不同编码、帧率和时间基准的视频转换为稳定、可复现的标准输入；
2. 先检测自然镜头边界，再仅在相邻 Shot 之间判断 Scene Boundary，缩小搜索空间；
3. 通过视觉语言模型理解场所与人物群体变化，通过字幕/大语言模型理解叙事事件的连续性，并预留视觉、音频特征模型；
4. 将模型结果转换成统一、归一化、可解释的 SceneEvidence；
5. 通过候选点、`scene_score` 和带最小间隔约束的选择算法生成最终场景；
6. 通过 Celery、Artifact、Manifest、模型版本和结构化日志保证长任务可追踪、可重试、可缓存和可单独重跑；
7. 为后续接入 SceneSeg、BaSSL 或其他模型保留稳定接口，而不改变上层 API 和最终 Schema。
8. 对 BDF/EDF 前额 EEG 进行质量可控的预处理，在统一播放时间轴上形成 Attraction、Engagement、Fatigue、Workload 四指标与群体 ISC；
9. 将 EEG 异常窗口聚合到 Scene 内部，区分“内容边界强度”和“观众价值”，生成带来源、阈值、持续时间和置信依据的保留/删除建议；
10. 将建议区间吸附到合法 Scene/Shot 边界，经人工复核或自动策略确认后调用视频执行模块，输出连续、可播放、可追溯的剪辑完成视频。

### 2.2 总体技术路线

系统采用六层架构：API 层、编排层、Worker 层、模型适配层、存储层和 Schema 层。核心处理流程如下：

```text
上传原始电影
  → API 创建 video_id / task_id
  → FFmpeg 标准化：normalized.mp4 + audio.wav + metadata.json
  → Shot 检测：OmniShotCut（主）或 FFmpeg scene filter（轻量备选）
  → shots.json
  → 并行执行：
       ├─ Doubao ASR/Whisper Adapter → subtitles.json
       ├─ PyAV 单次解码 → 每 Shot 的 25%/75% 关键帧
       └─ 音频/视觉特征提取（规划接口）
  → 对 N 个 Shot 形成 N-1 个候选边界
  → Qwen2.5-VL：判断场所与人物群体连续性
  → DeepSeek：从字幕形成分层叙事事件，转换为字幕连续性证据
  → SceneSeg/BaSSL：提供场景边界候选置信度（候选模型，待选型）
  → 统一 SceneEvidence + scene_score
  → Greedy/约束选择算法确定最终切点
  → 合并连续 Shot 为 Scene
  → final_result.json + Artifact Manifest + 数据库索引
  → 同步导入单被试/多被试 BDF/EDF EEG
  → DC 去除 + IIR 0.5-45Hz + 50Hz Notch + 眼电/振幅伪迹处理
  → 12s 窗/2s 步长：Att + Eng + Fat + WL
  → 12s 窗/6s 步长：leave-one-out 群体 ISC
  → EEG 窗口与 Scene 时间区间重叠聚合
  → 删除候选：低 ISC ∪ [Eng↓ ∨ (Att↓ ∧ WL↑ ∧ Fat↑)]
  → 相邻建议合并 + 短片段过滤 + Scene/Shot 边界吸附
  → GUI/Agent 人工复核或自动确认
  → FFmpeg 保留区间拼接 → edited_video.mp4 + edit_manifest.json
```

其中，模型不得自行操作 API、数据库或最终结果；模型间不得直接调用，所有先后关系和并行关系均由编排层控制。

### 2.3 六层系统架构

#### 2.3.1 API 层

API 层位于 `apps/api/`，使用 FastAPI 提供 `/api/v1/` 前缀的接口。其职责仅包括上传视频、校验请求、创建任务、返回 `task_id`、查询任务状态和读取结果。API 请求内不执行长时间 FFmpeg 或模型推理，也不使用 FastAPI BackgroundTasks 替代 Celery 主流程。

典型接口包括：

- `POST /api/v1/videos`：创建视频记录并保存原始文件；
- `POST /api/v1/videos/{id}/analysis`：创建分析任务并触发编排；
- `GET /api/v1/tasks/{id}`：查询状态、阶段、进度和错误；
- `GET /api/v1/videos/{id}/results`：读取最终结果。

#### 2.3.2 编排层

编排层位于 `core/orchestration/` 与 `pipelines/`，负责决定任务顺序、并发关系、缓存复用和失败传播。推荐的任务图为：

```text
video.normalize
  → shot.detect
  → group(video.extract_keyframes, subtitle.transcribe, feature.extract_audio)
  → scene.detect_boundaries / scene.collect_evidence
  → scene.merge_shots
  → scene.calculate_score
  → final.assemble
  → final.pipeline_complete
```

只有 `final.pipeline_complete` 可以把总任务状态置为 `SUCCEEDED`；中间任务只更新各自的 ModelRun、Artifact 和进度。

#### 2.3.3 Worker 层

Worker 层位于 `workers/`，基于 Celery 将 CPU 与 GPU/API 任务分开路由。系统预设七个队列：

| Queue | 主要任务 | 资源类型 |
|---|---|---|
| `video` | 视频规范化、代理视频、关键帧与音频预处理 | CPU / I/O |
| `shot` | OmniShotCut 或 FFmpeg Shot 检测 | CPU，可选 GPU |
| `subtitle` | ASR 转录与字幕解析 | API / CPU |
| `feature` | 视觉与音频特征提取 | GPU / CPU |
| `scene` | VLM/LLM 证据提取、Scene 候选与评分 | API / GPU / CPU |
| `final` | 合并、装配和最终状态更新 | CPU |
| `maintenance` | 临时文件、缓存和维护任务 | CPU / I/O |

Celery 配置采用 `task_track_started=True`、`task_acks_late=True`、`worker_prefetch_multiplier=1`、`task_reject_on_worker_lost=True` 和启动时 Broker 重连。临时网络、对象存储和 Redis 故障允许有限重试；视频损坏、Schema 错误、权重不兼容和 CUDA 环境错误不进行无限重试。

#### 2.3.4 模型适配层

所有模型继承 `models/base/adapter.py` 中的统一 BaseModelAdapter，并实现加载、预测、健康检查和卸载接口。上层只认识标准输入外壳和 Artifact URI，不直接依赖第三方 SDK。

统一输入外壳包含：

```json
{
  "schema_version": "1.0",
  "task_id": "task_001",
  "video_id": "video_001",
  "model": {"name": "omnishotcut", "version": "1.0.0"},
  "input": {"video_uri": "storage://.../normalized.mp4"},
  "parameters": {"mode": "clean_shot"}
}
```

成功输出统一包含 `status`、`model`、`artifacts`、`metrics.runtime_ms` 和空的 `error`；失败输出包含标准错误码、可读消息和 `retryable` 标志。

#### 2.3.5 存储层

数据库只保存视频、任务、模型运行和 Artifact 的元数据，不保存整部视频或大型特征矩阵。大型文件写入本地存储或 S3 兼容对象存储，下游仅通过 URI 读取。

Artifact 路径遵循：

```text
storage://projects/{project_id}/videos/{video_id}/{category}/{model_name}/{version}/{filename}
```

每个主要 Artifact 通过 `core/artifacts/writer.py` 先写临时文件，再原子重命名，并生成 `.manifest.json`。Manifest 记录 Artifact ID、video_id、run_id、生产模型、版本、SHA-256、大小和创建时间，避免缓存污染和旧结果被静默覆盖。

#### 2.3.6 Schema 层

跨模块数据统一使用 `schemas/` 下的 Pydantic 模型。时间使用整数毫秒；数值特征、置信度和 `scene_score` 均限制在 `[0,1]`。正式 SceneEvidence 只允许：

- `visual_continuity`
- `character_continuity`
- `location_continuity`
- `subtitle_continuity`
- `audio_continuity`
- `temporal_gap_ms`

系统对外只计算并输出 `scene_score`，不建立相互竞争的多套最终评分。

### 2.4 EEG 片段价值评估与剪辑执行层

融合系统在六层视频分析架构之后增加一个与其解耦但共享时间轴和 Artifact 规则的 EEG 价值评估子系统。该子系统由 `movie_EEG/eeg_core` 提供确定性算法，由 CLI、PySide6 GUI 和 LLM Agent 三种入口共同调用，避免三个交互端分别实现不同算法。

#### 2.4.1 EEG Core 共享算法包

`eeg_core` 包含以下模块：

| 模块 | 主要职责 | 与视频系统的接口 |
|---|---|---|
| `io` | 读取 EDF/BDF，建立 Raw 容器，将 V 转换为 μV | 接收 EEG URI、subject_id、trial/run 与播放时间信息 |
| `channel_mapping` | ND8 到 10-20 映射、双极导联、Fpz 与 FAA 通道对解析 | 生成可追溯的通道配置 |
| `preprocessing` | DC 去除、IIR 滤波、伪迹修复和可选 Fpz 重参考 | 输出预处理缓存与质量元数据 |
| `artefact_detection` | 眼电和超过 100 μV 的振幅伪迹检测、线性插值 | 形成窗口 Quality 和 ISC 有效样本掩码 |
| `scoring` | FFT 周期图、δ/θ/α/β/γ 功率、Att/Eng/Fat/WL | 输出每个滑动窗口的四指标与质量 |
| `sync_rate` | 多被试 leave-one-out ISC、Fisher-z 聚合 | 输出群体同步总体值和时间序列 |
| `edit_suggestion` | 多指标规则、低 ISC 规则、片段合并与过滤 | 输出建议删除的时间区间和触发来源 |

#### 2.4.2 三种使用入口

- **CLI**：适合逐 run、trial、subject 的批量分析和研究复核；
- **GUI**：包含 Preview、PSD、Score、ISC、Edit 五个 Tab，可查看波形、伪迹、频谱、四指标、群体同步和剪辑时间轴；
- **LLM Agent**：通过自然语言调用记录检查、质量检查、时间段分析、Phase 分析、极值查找、PSD、Phase 对比、群体 ISC、结构化导出和 `suggest_run_edits` 等高层工具。

LLM Agent 只负责理解用户意图、选择工具和解释确定性结果，不替代 `eeg_core` 算法，也不允许凭语言模型主观判断删除片段。

#### 2.4.3 跨项目编排边界

视频系统输出 Scene 时间轴；EEG 系统输出基于播放时间的异常区间。融合编排器负责把二者映射、聚合并形成剪辑决策。EEG 子系统不修改原始 Scene 结果，视频分段系统也不直接解释脑电生理含义。这样可分别升级 VLM、Shot 模型或 EEG 规则，而不破坏另一侧的可复现性。

---

## 三、核心算法与数据处理方法

### 3.1 视频标准化与时间基准统一

原始视频可能采用 VFR、不同像素格式、异常 PTS 或不兼容封装。系统首先调用 FFprobe 获取原始时长、帧率分数、帧数、编解码器、像素格式、音频流和起始时间，再由 FFmpeg 生成 H.264、`yuv420p`、恒定帧率的 MP4，并按需要生成 16 kHz 单声道 WAV。

标准化后执行以下校验：

1. 输出存在且大小非零；
2. 视频流和编解码器可读；
3. 时长差不超过配置阈值或一个原始帧时长；
4. 帧数、FPS 分子和分母均有效；
5. 起始时间接近 0 ms；
6. 像素格式和封装满足配置；
7. 保存转换前后 FFprobe、FFmpeg 版本、命令、输入/输出 SHA-256 和校验结果。

帧号与毫秒的转换采用整数运算：

```text
timestamp_ms = floor((frame × fps_den × 1000 + fps_num / 2) / fps_num)
```

所有区间统一为 `[start_ms, end_ms)`；若第三方模型输出闭区间帧号 `[start_frame, end_frame]`，Adapter 必须转换为 `end_frame_exclusive = end_frame + 1`，从源头消除边界重叠。

### 3.2 Shot Boundary Detection

#### 3.2.1 OmniShotCut 主检测器

OmniShotCut 仅负责识别 hard cut、dissolve、wipe 等自然镜头边界，不直接判断 Scene，也不计算最终评分。当前项目将其固定到提交 `23ad6fb41b296fb9258b0e7825125a914573b906`，权重文件为 `OmniShotCut_ckpt.pth`，并记录 SHA-256。Adapter 负责：

1. 从版本化 `model_store` 加载一次权重并在 Worker 生命周期内复用；
2. 只读取 `normalized.mp4`，不读取原始视频；
3. 将第三方帧区间转换为项目 Shot Schema；
4. 生成 `shot_id`、顺序索引、整数毫秒、帧区间、边界类型和归一化置信度；
5. 校验 Shot 是否从 0 开始、相邻、无重叠、覆盖有效时长且不含禁止字段；
6. 保存 `shots.json`、原始输出、模型运行指标和 Manifest。

#### 3.2.2 FFmpeg 轻量备选

在无法加载 PyTorch/权重或需要快速初筛时，可使用 FFmpeg scene filter 形成与 OmniShotCut 相同格式的 `shots.json`。该模块是 Shot 检测替代器，不改变下游 Contract。这样既可用于降级运行，也可在评测中作为传统基线。

### 3.3 候选空间压缩：只在 Shot 之间判断 Scene

设视频包含按时间排序的 Shot 集合：

```text
S = {s1, s2, ..., sN}
```

系统只评估相邻 Shot 之间的 `N-1` 个候选边界：

```text
bi = boundary(si, s(i+1)), i ∈ [1, N-1]
```

边界 ID 直接使用其前一个 Shot 的 `shot_id`。例如 `shot_000105` 表示 `shot_000105 → shot_000106` 之间的边界。VLM、字幕语义模块、特征模型、选择算法和最终结果都使用同一 ID，避免额外索引换算和 off-by-one 错误。

与每 0.2 秒密集采样相比，本方法天然把最终切点限制在可解释的自然镜头切换处，并显著减少多模态模型调用次数。

### 3.4 关键帧抽取

每个 Shot 选取 25% 与 75% 两个位置的代表帧，分别用于表示 Shot 的前段和后段。对边界 `si → s(i+1)`，VLM 比较 `si` 的 75% 帧和 `s(i+1)` 的 25% 帧。

目标帧采用整数比例计算：

```text
span = end_frame_exclusive - start_frame
offset = floor((numerator × (span - 1) + denominator / 2) / denominator)
target = min(start_frame + offset, end_frame_exclusive - 1)
```

工程实现不是对每个 Shot 反复 seek，而是：

1. 预先计算全部目标帧；
2. 按帧号全局去重并排序；
3. 使用 PyAV 对视频进行一次顺序解码；
4. 命中目标帧时缩放并编码 JPEG/PNG；
5. 使用临时文件加原子重命名保存；
6. 计算图片 SHA-256，并用 PTS 与期望毫秒校验。

VLM 可优先使用 320×180 代理图降低传输和推理成本；在当前 Adapter 中，320 px 图可配置较大批次，而 672 px 图自动缩小批次，避免请求体和上下文超限。

### 3.5 字幕生成与时间对齐

字幕模块通过统一 WhisperAdapter 接口接入当前的 Doubao ASR Provider。输入可为标准化视频或 16 kHz 单声道 WAV；长音频可按时长分片并并行转录，合并时把每片段的相对时间戳恢复到全片整数毫秒坐标。

输出 `subtitles.json` 中每个 SubtitleSegment 包含 `subtitle_id`、`start_ms`、`end_ms`、文本、语言和可用时的置信度。字幕只作为 Scene 边界证据，不能把字幕断句直接当作最终切点；当 ASR 只能返回整段文本而没有 utterance 级时间戳时，应降低其边界证据权重或标注不确定性。

### 3.6 VLM 场所与人物连续性判断

Qwen2.5-VL Adapter 以边界两侧图像为输入，对每个候选边界同时判断：

1. **场所连续性**：室内/室外、房间类型、空间布局、光照环境和背景结构是否保持一致；
2. **人物连续性**：边界两侧是否仍为同一组人物，能够容忍对话正反打、同一人物特写、全景/近景切换和同一场景换机位。

VLM 原始响应应包含 `shot_id`、场所变化判断、人物群体变化判断和简短理由。进入正式 Schema 前，变化量转换为连续性：

```text
location_continuity  = 1 - normalize(location_change)
character_continuity = 1 - normalize(character_group_change)
```

其中 `normalize(·)` 把模型输出校准到 `[0,1]`。为避免不同批次评分尺度漂移，应使用固定 Prompt、低温度、跨批锚点样例，并在验证集上进行分位数或等距校准；仅在单批内做 min-max 归一化会放大异常值，不宜作为唯一校准手段。

### 3.7 字幕语义与叙事事件连续性

DeepSeek 文本模型读取带时间戳的完整字幕，规划 major、medium、minor 三层叙事事件：major 对应较大的幕或阶段，medium 对应该阶段中的子任务，minor 对应局部叙事节拍。事件起止时间再映射到最近的 Shot 候选边界。

该模型的正式职责是产生“字幕/叙事是否连续”的内部证据，不直接输出最终切点。映射原则为：

- 同一 minor 事件内部：`subtitle_continuity` 较高；
- minor 事件切换：连续性小幅下降；
- medium 事件切换：连续性明显下降；
- major 事件切换：连续性最低；
- 无字幕或字幕时间置信不足：该维度记为缺失，不强行置零。

项目现有实验任务中仍保留了早期叙事变化中间字段。正式集成时必须在 Adapter/转换层将其收口为允许的 `subtitle_continuity`，最终输出只保留 `scene_score` 和 SceneEvidence。

### 3.8 视觉、音频与候选 Scene 模型

系统为以下模型保留独立 Adapter 和 Artifact 接口：

- **Visual Encoder**：按 Shot 提取视觉 embedding，供相邻 Shot 计算 `visual_continuity`；
- **Audio Encoder**：提取音乐、环境声或声学 embedding，供相邻时间窗计算 `audio_continuity`；
- **SceneSeg/BaSSL**：作为 Scene Boundary 候选模型，输出边界候选置信度，不直接决定最终切点。

SceneSeg/BaSSL 目前仍处于候选选型状态，视觉和音频编码器在 I/O Contract 中已有接口但尚未形成完整生产实现。因此它们应在实验完成、License 核验、Registry 登记和回归测试通过后再启用。

### 3.9 SceneEvidence 与唯一 Scene Score

对候选边界 `bi`，定义可用证据集合：

```text
Ci = {
  visual_continuity,
  character_continuity,
  location_continuity,
  subtitle_continuity,
  audio_continuity,
  temporal_gap_ms
}
```

连续性越低，边界越可能是新 Scene。可定义每个维度的不连续度：

```text
di,k = 1 - ci,k
```

对缺失证据不以 0 填充，而是在可用维度上重新归一化权重。边界的唯一评分可写为：

```text
scene_score(i) = clamp(
  (Σk∈Ai wk × di,k) / (Σk∈Ai wk)
  + wt × temporal_prior(i),
  0, 1
)
```

其中：

- `Ai` 为边界 `i` 实际可用的证据维度；
- `wk` 为非负权重；
- `temporal_prior(i)` 是由 `temporal_gap_ms` 映射到 `[0,1]` 的时间先验；
- `clamp` 将结果限制到 `[0,1]`。

该设计有三点作用：第一，统一“连续性证据”的方向；第二，允许无字幕或无音频等缺失模态优雅降级；第三，保证系统只产生一种可比较的最终 Scene 评分。

### 3.10 约束选点与 Shot 合并

模型只能生成候选证据。最终切点由编排层执行带约束的选择算法：

```text
输入：按 scene_score 降序排列的 N-1 个候选边界
约束：目标 Scene 数 K、相邻切点最小间隔 min_distance_ms

selected = []
for boundary in ranked_boundaries:
    if boundary 与 selected 中任一切点距离小于 min_distance_ms:
        continue
    selected.append(boundary)
    if len(selected) == K:
        break
按 timestamp_ms 对 selected 重新升序排列
```

然后将相邻切点之间的连续 Shot 合并成 Scene。每个 Scene 记录 `scene_id`、`video_id`、索引、`start_ms`、`end_ms`、有序 `shot_ids`、边界置信度和 `[0,1]` 的 `scene_score`。

可进一步加入以下工程约束：

- 最短/最长 Scene 时长；
- 强制保留片头与片尾；
- 同分候选时优先选择多模态证据更完整者；
- 对交叉剪辑允许通过回看窗口和迟滞机制避免频繁开合；
- 对极短 Shot 集群先做局部聚合，再进入全局 Greedy。

### 3.11 EEG 数据组织、同步与通道映射

EEG 数据按 `run/trial/subject` 三层组织，每个被试使用 `.bdf` 或 `.edf` 文件，可附带定标日志或 Phase 定义。系统至少保存 `run_id`、`trial_id`、`subject_id`、采样率、通道名称、参考方式、播放起始偏移和数据文件哈希。

当前 ND8 映射为：CH1→AF4、CH2→AF8、CH3→Fp2、CH4→Fpz、CH5→Fp1、CH6→AFz、CH7→AF3、CH8→AF7。Attraction 的额叶 Alpha 不对称优先使用 AF4-AF3、AF8-AF7、Fp2-Fp1 三对通道；Workload 和 Fatigue 优先使用 Fpz，若进行 Fpz 重参考后通道变化，则按代码回退到 Fp1/Fp2 平均或适配双极导联。

EEG 时间与视频时间建立如下关系：

```text
video_timestamp_ms = eeg_sample_index × 1000 / eeg_fs + playback_offset_ms
```

工程实现应使用整数或有界舍入完成样本到毫秒的转换，并记录 `playback_offset_ms` 的来源。若播放器存在暂停、跳转、缓冲或倍速，应使用事件日志构建分段单调同步映射，而不能只用一个固定偏移。没有可靠同步日志的数据只能用于整场统计，不应生成帧级自动剪辑决定。

### 3.12 EEG 预处理与质量门控

当前共享预处理管线为：

```text
原始 EEG（V→μV）
  → 每通道 DC 去除
  → IIR 高通 0.5 Hz
  → IIR 低通 45 Hz
  → 50 Hz Notch
  → 眼电检测：12 Hz 低通 + 自适应阈值 + 半峰值边界
  → 振幅检测：|signal| > 100 μV
  → 眼电并集 ∪ 逐通道振幅伪迹
  → 线性插值修复
  → 可选 Fpz 重参考
```

眼电峰之间小于约 300 ms 时合并；半峰值边界限制在约 50-600 ms 范围。对一个评分窗口，定义：

```text
Quality = 1 - 该窗口内伪迹采样点比例
```

`Quality ≥ 0.7` 的窗口参与累积均值和方差基线；`Quality < 0.7` 的窗口仍保留输出并标记低质量，但不更新基线。剪辑器不得把低质量 EEG 本身解释为“观众不喜欢”，必要时应要求人工复核或使用其他被试的有效数据。

### 3.13 频谱估计与四项个体/组平均指标

评分采用 12 秒窗口、2 秒步长。每个窗口乘 Hanning 窗后执行实数 FFT，按周期图公式计算功率谱密度，再汇总 δ、θ、α、β、γ 频段功率。当前代码形成四项指标：

#### 3.13.1 Engagement（Eng，投入度）

```text
Eng_raw = 1/2 × [ln(β/α) + ln(β/(α+θ))]
```

Engagement 以 session 内 1% 和 99% 分位点截断后归一化到 `[0,1]`。它表达同一被试在当前观看 session 中的相对投入水平，而不是跨人群可直接比较的绝对量表。

#### 3.13.2 Attraction（Att，趋近度）

```text
FAA = mean[ln(α_R) - ln(α_L)]
```

对三对前额通道求均值，并通过随时间累积的基线转换为 z-score。低 Att 表示相对趋近动机下降，但单独的 Att 变化可能受个体差异和参考方式影响，因此当前多指标规则不让其独立触发删除。

#### 3.13.3 Workload（WL，认知负荷）

```text
WL_raw = ln(θ) - ln(α)
```

在 Fpz 或回退前额通道上计算，再转为累积 z-score。高 WL 可能表示理解成本或认知压力上升；低 WL 可能表示任务过于简单，也可能只是轻松观看，因此需要结合其他指标解释。

#### 3.13.4 Fatigue（Fat，疲劳度）

```text
Fat_raw = ln(θ) + ln(α)
```

使用与 WL 相同的前额通道并转换为累积 z-score。高 Fat 与低 Att、高 WL 同时出现时，构成更强的删除候选证据。

Attraction、Workload 与 Fatigue 使用 Welford 等价的在线累积均值/方差机制；只有 Quality 合格的窗口更新基线。多被试模式先为每个被试独立完成归一化，再对同一时间窗口求组平均，避免某个被试的原始幅值主导结果。

### 3.14 群体 ISC（Inter-Subject Correlation）

ISC 用于衡量多个观众在同一时间段内是否产生相似的神经响应。系统对 EEG 低通至 20 Hz，按通道仅使用干净样本进行 z-score，并在 12 秒窗口、6 秒步长上采用 leave-one-out 方式：

```text
对每个目标被试 t、每个通道 c：
  GA(-t,c) = 其余有效被试在通道 c 上的逐样本平均
  r(t,c)   = Pearson(EEG(t,c), GA(-t,c))
对 r 执行 Fisher-z 变换后求平均，再反变换得到窗口 ISC
```

代码预先把多被试信号堆叠为三维数组，向量化计算总和，再对目标被试做“总和减自身”，减少重复平均开销。窗口内某通道的有效样本比例不足 0.7 时跳过该相关；至少需要两名被试，人数越少时结果不确定性越高。

ISC 极低说明观众响应缺乏同步，但不必然等同于内容质量差。例如开放式思考、个体化偏好或数据噪声也会降低 ISC。因此本系统保留 ISC 的独立触发来源和 Quality 证据，允许编辑者复核。

### 3.15 五类 EEG 证据的剪辑建议规则

融合系统把 Att、Eng、Fat、WL 与 ISC 统称为五类片段价值证据，其中前四项来自组平均窗口指标，第五项来自多被试同步。当前 `detect_pipeline_clips` 的实际规则为：

```text
s1 = ISC < μISC - 2.0σISC

E  = Eng < μEng - 1.2σEng
A  = Att < μAtt - 1.2σAtt
HW = WL  > μWL
F  = Fat > μFat + 1.2σFat

B = E ∨ (A ∧ HW ∧ F)
FinalCandidate = B ∪ s1
```

规则含义如下：

| 证据 | 阈值 | 是否独立触发 | 解释 |
|---|---|---|---|
| Eng↓ | `< μ-1.2σ` | 是 | 群体相对投入显著降低 |
| Att↓ | `< μ-1.2σ` | 否 | 需与高负荷、高疲劳共同出现 |
| WL↑ | `> μ` | 否 | 与 Att↓、Fat↑组成疲劳性动机下降模式 |
| WL↓ | `< μ` | 当前不参与 B | 作为可视化和解释证据保留 |
| Fat↑ | `> μ+1.2σ` | 否 | 需与 Att↓、WL↑共同出现 |
| ISC↓ | `< μ-2.0σ` | 是 | 群体同步极低；仅保留持续时间足够的片段 |

现有策略文档记录了特定历史 run 的 permutation 检验结果，但该统计结论不能直接外推到所有影片和人群。正式产品应在独立数据集上重新验证 1.2σ、2.0σ、持续时间和逻辑组合。

### 3.16 建议窗口合并与 Scene 边界对齐

当前 EEG 代码先把相邻候选窗口合并为片段，再把间隔不超过 6 秒的片段继续合并；仅由低 ISC 触发的片段需持续至少 10 秒，B 规则触发的片段不受该限制。

融合到电影系统后增加 Scene 约束层。设 EEG 建议区间为 `E=[e_start,e_end)`，Scene 为 `Sj=[s_start,s_end)`，重叠率定义为：

```text
overlap(E,Sj) = duration(E ∩ Sj) / duration(Sj)
```

系统可按以下策略之一处理：

1. **整 Scene 删除**：当一个 Scene 的低价值窗口覆盖率超过配置阈值，且该 Scene 不包含强制保留事件时，建议删除整个 Scene；
2. **Scene 内部细剪**：当低价值窗口只覆盖 Scene 局部时，把起止时间吸附到最近 Shot Boundary，禁止在 Shot 内部任意切断；
3. **保守扩展**：若 EEG 窗口跨越多个 Scene，分别计算各 Scene 证据，不把 12 秒窗口无条件扩展成全部 Scene；
4. **冲突保护**：当 `scene_score` 表明边界清晰但 EEG 仅有低质量单点异常时，优先保留并请求人工复核；
5. **叙事保护**：片头、片尾、字幕关键事件或用户锁定区间可设置 `protected=true`，自动策略不得删除。

因此，内容模型负责“在哪里可以自然切”，EEG 模型负责“哪些内容可能不值得保留”，二者通过约束映射共同形成最终剪辑决定。

### 3.17 剪辑建议 Schema 与证据链

建议新增独立的 EEG/Edit Artifact，而不修改视频侧 SceneEvidence 的既有字段约束。一个剪辑建议至少包含：

```json
{
  "edit_id": "edit_000001",
  "video_id": "video_001",
  "start_ms": 120000,
  "end_ms": 156000,
  "decision": "suggest_delete",
  "aligned_scene_ids": ["scene_000008"],
  "aligned_shot_ids": ["shot_000105", "shot_000106"],
  "sources": ["low_engagement", "low_isc"],
  "eeg": {
    "n_subjects": 7,
    "quality_valid_ratio": 0.91,
    "att": -1.31,
    "eng": 0.12,
    "fat": 1.42,
    "wl": 0.67,
    "isc": -0.08
  },
  "parameters_uri": "storage://.../eeg_edit_config.json",
  "review_status": "pending"
}
```

示例数值只用于说明字段结构，不代表项目实测。正式数据中 Att/Fat/WL 可保留 z-score，Eng 保留 `[0,1]` session 相对值，ISC 保留相关系数及时间 z-score；每个字段必须附带计算版本、参考方式、窗口、步长和质量信息。

### 3.18 从剪辑建议到成片输出

经过自动规则或人工复核后，把所有 `suggest_delete` 区间规范化为互不重叠的删除集合 `D`，再求其相对于完整视频区间 `V=[0,duration_ms)` 的补集，得到有序保留区间：

```text
K = V \ union(D)
```

视频执行模块逐个截取 `K` 中的区间，再按原时间顺序拼接。为保证成片质量，应：

1. 把裁切点限制在已选 Shot/Scene Boundary 或可安全重编码的位置；
2. 视频与音频使用同一时间区间，避免声画漂移；
3. 对非关键帧起点采用重编码或准确 seek，不以粗略 stream copy 冒充精确裁切；
4. 统一分辨率、帧率、像素格式、音频采样率和声道；
5. 输出 `edited_video.mp4`、`edit_decisions.json`、EDL/CSV、FFmpeg 命令、输入输出 SHA-256 和 `edit_manifest.json`；
6. 保留原始视频和全部中间 Artifact，不覆盖旧成片版本；
7. 在无有效保留区间、建议覆盖率过高或媒体校验失败时停止执行并请求人工确认。

当前 `movie_EEG` 已实现建议区间、时间轴和 CSV/JSON 导出；实际 FFmpeg 成片执行应接入 Movie Analysis Platform 的媒体与 Artifact 层后完成。

---

## 四、硬件模组（保留原文内容）

> 本节按用户要求保留参考文档的硬件内容。融合系统中的电影 Scene 分段可以独立运行；当需要进一步判断片段是否值得删除时，接入 EEG 数据执行观众价值评估。PPG、IMU 与环境光仍属于可选辅助传感器。当前 `movie_EEG` 代码除本节双通道前额方案外，还支持 ND8 八通道映射和部分双极导联，实际使用时必须按设备记录通道布局与参考方式。

### 4.1 头戴式 EEG 传感器

- 佩戴方式：头戴式支架，类似于轻量化头环或眼镜框架、或者是空顶帽，佩戴于用户额头。
- 电极配置：
  - 双通道干电极：分别对应国际 10-20 系统的 FP1 和 FP2 位置（额极区），用于采集额叶区脑电信号。
  - 参考电极：设置于额头正前方中线 FPz 位置。
  - 地电极：集成于头戴支架与皮肤接触处（也可以和参考电极合成一个，减少电极）。
- 采样参数：采样率 ≥256 Hz，分辨率 24 bit，支持 θ 波（4-8 Hz）、α 波（8-13 Hz）、β 波（13-30 Hz）的功率谱密度分析。

### 4.2 辅助传感器

| 传感器类型 | 型号示例 | 功能作用 |
|---|---|---|
| 心率传感器 | 光电容积脉搏波传感器 | 采集心率及心率变异性（HRV），辅助判断情绪唤起状态 |
| 加速度传感器 | 六轴 IMU（惯性测量单元） | 检测头部微动，用于后期筛选平稳画面以及运动伪迹补偿 |
| 环境光传感器 | RGBW 颜色传感器 | 检测环境色温、亮度，用于视频色彩统一校正 |

### 4.3 相机本体

- 光学单元：支持 4K/60fps 录制，具备光学防抖功能。
- 处理器：内嵌 NPU（神经网络处理单元）的移动端 SoC，用于实时推理大模型。
- 存储单元：支持循环缓存的预录制缓冲区（可存储触发前 15 秒视频）。
- 说明：相机不限于以上规格，可以使用智能手机、现有运动相机、智能眼镜作为替代。

### 4.4 硬件选型示例

- EEG 传感器芯片：2 通道低功耗生物电位测量前端；
- 主控芯片：Rockchip RK3588（集成 6 TOPS NPU）；
- 心率传感器：MAX30102（光电容积脉搏波传感器）；
- 加速度计：BMI270（六轴 IMU）；
- 环境光传感器：VEML7700（环境光 + 色温检测）。

### 4.5 电极放置位置

采用干电极设计，无需导电膏：

- FP1 电极：位于左眼正上方约 2 cm 处；
- FP2 电极：位于右眼正上方约 2 cm 处；
- 参考电极 FPz：位于额头正中央、发际线下方；
- 地电极：集成于头戴支架后部与皮肤接触处。

### 4.6 原硬件信号处理参数

1. EEG 信号预处理：0.5-45 Hz 带通滤波；50 Hz 陷波滤波；采用独立成分分析去除眼动伪迹。
2. 特征提取：采用 Welch 方法计算 θ、α、β 波相对功率；提取 RR 间期与 LF/HF 比值。
3. 模型推理输入维度：`[时间窗口长度 3 秒 × 采样率 256 Hz] × 特征维度`。
4. 推理频率：2 Hz，即每 0.5 秒输出一次结果。
5. 原文默认阈值：80 分，可手动调节；自适应阈值可设为一周平均值加 1.5 倍标准差。

### 4.7 与 movie_EEG 软件的硬件兼容关系

原参考文档的 FP1/FP2/FPz 双通道/参考电极方案继续作为轻量采集实施例保留；当前软件的主要 ND8 映射则覆盖 AF4、AF8、Fp2、Fpz、Fp1、AFz、AF3、AF7，可提供三对 FAA 与独立 Fpz 信息。软件通过通道名称和通道数自动判断 montage，并为双通道、ND8 和双极导联选择不同的 FAA/Workload 回退策略。

为保证指标可比性，每次实验必须保存设备型号、固件、采样率、ADC 分辨率、电极位置、参考电极、阻抗或接触质量、丢包、播放设备和同步方式。不同 montage 之间不得直接复用同一数值基线，需分别校准和验证。

---

## 五、具体实施方式

### 5.1 实施例一：标准电影分析任务

1. 用户通过 `/api/v1/videos` 上传电影，系统生成 `project_id` 与 `video_id`，原始文件保存到版本化存储路径。
2. 用户提交分析请求，API 生成 `task_id` 后立即返回，不等待模型推理。
3. `video.normalize` 调用 FFprobe 与 FFmpeg 形成 `normalized.mp4`、`audio.wav`、媒体元数据、前后探测记录和 Manifest。
4. `shot.detect` 加载 OmniShotCut，输出符合 Shot Schema 的 `shots.json`。若主模型不可用，可切换 FFmpegSceneAdapter 作为降级检测器。
5. 编排层并行启动关键帧抽取与字幕转录；大型数组或图片不进入 Redis，只传 task_id、video_id 和 URI。
6. 关键帧 Worker 单次顺序解码全片，生成每个 Shot 的 25% 与 75% 图像及哈希记录。
7. 字幕 Worker 通过 Doubao ASR Provider 生成整数毫秒字幕；若音频超过配置时长则分片并行。
8. VLM Worker 以相邻 Shot 的边界帧对调用 Qwen2.5-VL，形成场所和人物连续性证据及理由。
9. 文本模型根据字幕构建大/中/小叙事事件树，并将事件关系映射为 `subtitle_continuity`。
10. 若视觉/音频编码器和 SceneSeg/BaSSL 已启用，则并行补充对应证据；未启用时评分器只在可用模态上重新归一化权重。
11. 评分器计算每个候选边界唯一的 `scene_score`，选择算法应用目标数量与最小间隔约束。
12. 系统把连续 Shot 合并为 Scene，写入 `scenes.json`、`scene_evidence.json`、`scene_scores.json` 和最终 `final_result.json`。
13. `final.pipeline_complete` 在所有上游成功后将任务更新为 100% 和 `SUCCEEDED`；任一失败均保留错误码、run_id 和已有 Artifact 以便重跑。
14. 播放实验结束后导入与该视频对应的多被试 BDF/EDF，读取 run、trial、subject、通道和同步偏移。
15. 对每名被试执行 EEG 预处理、伪迹质量计算、12 秒/2 秒四指标评分，并读取或生成 ear/fpz 隔离的缓存。
16. 对至少两名有效被试执行 leave-one-out ISC，形成窗口同步时间序列。
17. 运行 `detect_pipeline_clips`，得到低 ISC 与多指标规则触发的建议区间；将秒转换为整数毫秒并映射到 Scene/Shot 边界。
18. GUI Edit Tab 显示十行时间轴、Phase 分界和建议来源；LLM Agent 可调用 `suggest_run_edits` 生成自然语言说明，但最终区间来自确定性算法。
19. 用户确认后，视频执行模块计算保留区间、调用 FFmpeg 裁切拼接并校验声画时长，保存新版本 `edited_video.mp4` 与剪辑 Manifest。

### 5.2 实施例二：无对白片段

当某段电影没有字幕时，系统不把 `subtitle_continuity` 强制设为 0，而是标记该证据缺失。评分器在视觉、人物、场所、音频和时间证据上重新归一化权重。这样不会因为“没有对白”而错误制造高分边界。

### 5.3 实施例三：对话正反打

两个角色在同一房间交谈，镜头在双人全景、角色 A 特写和角色 B 特写之间切换。Shot 模型会检测到多个硬切；Qwen2.5-VL 识别同一场所和同一人物群体，字幕事件保持连续，因此各维度连续性较高，`scene_score` 低于选择阈值，系统把这些 Shot 合并为同一 Scene。

### 5.4 实施例四：真正换场

镜头从室内会议切到夜间街道，同时人物群体变化、背景环境声变化且字幕进入新的叙事阶段。多个连续性维度同时下降，产生较高 `scene_score`。若该候选与已选切点间隔满足约束，选择算法将其确定为 Scene Boundary。

### 5.5 实施例五：模型或 API 故障降级

若 Qwen2.5-VL 暂时不可用，任务记录 `VLM_INFERENCE_FAILED` 并按有限次数重试；若重试仍失败，可根据产品策略选择停止任务或使用本地视觉特征与 Scene 候选模型继续。无论哪种方式，都在结果中保留实际使用的模型、版本、证据缺失情况和运行指标，不伪造完整多模态结果。

### 5.6 实施例六：低投入片段的 Scene 级删除建议

某一 Scene 包含多个连续 Shot，组平均 Engagement 在大部分有效窗口内低于 session 均值减 1.2 倍标准差，同时 EEG Quality 合格。系统将这些窗口合并，并计算其对该 Scene 的覆盖率；覆盖率达到配置阈值后生成整 Scene 删除建议。由于裁切发生在 Scene 起止边界，不会从一句对白中间切开。

### 5.7 实施例七：疲劳性动机下降联合模式

某段中 Attraction 低于 `μ-1.2σ`、Workload 高于均值且 Fatigue 高于 `μ+1.2σ`，而 Engagement 未必达到独立低阈值。系统通过 `Att↓ ∧ WL↑ ∧ Fat↑` 识别“高认知负荷、疲劳上升、趋近动机下降”的联合模式，将该时间段标为候选，避免让任一可能含义模糊的指标单独决定删除。

### 5.8 实施例八：低群体同步与个体差异复核

当 ISC 低于 `μ-2.0σ` 且持续至少 10 秒时，系统生成低同步来源的独立建议。如果该段四指标并未异常，GUI 和 Agent 会保留“仅由低 ISC 触发”的来源标签，提示编辑者检查是否为开放式内容、受众分化或同步误差，而不是自动把低同步等同于低质量。

### 5.9 实施例九：建议区间跨越 Scene 边界

一个 12 秒 EEG 窗口可能横跨两个 Scene。系统分别计算它与两侧 Scene 的时间重叠率；若异常主要位于后一 Scene，则把建议起点吸附到该 Scene 的起点。若两侧证据不足，则保留为待人工复核，不用 EEG 窗口边缘直接裁切。

### 5.10 实施例十：剪辑完成视频的生成与回溯

用户确认若干删除建议后，系统合并重叠删除区间并计算补集，获得按时间排序的保留片段。FFmpeg 对保留片段执行精确裁切和 concat，输出成片；随后 FFprobe 验证视频流、音频流、时长、帧率和起始时间。Manifest 记录原视频哈希、决策列表、Scene/Shot 映射、EEG 参数、FFmpeg 命令和成片哈希，从而可以复现同一剪辑版本。

---

## 六、创新点总结

### 6.1 从“密集时间采样”转向“自然 Shot 边界上的多模态判别”

最接近的常见方案是在固定时间间隔上抽帧并判断变化，但会产生大量无意义候选点，最后还需把时间吸附回镜头边界。本方案先用 OmniShotCut 形成自然 Shot，再只评估 `N-1` 个相邻边界。该改变同时提高切点的电影语义合理性、降低 VLM 调用规模，并消除二次吸附误差。

### 6.2 边界 ID 全链路复用

每个候选边界用其前一个 `shot_id` 标识。视觉、字幕、音频、候选模型、评分器和最终 Scene 都使用相同键连接，避免多模型分别维护帧号、数组下标与时间戳造成的错位。这是面向多模型工程集成的关键机制创新。

### 6.3 “模型提证据、算法定切点”的职责分离

传统端到端方案往往让模型直接输出最终切点，难以加入时长、数量、相邻距离和业务约束。本方案把模型输出限制为候选置信或连续性证据，最终由可审计的选择算法决策。因而可独立替换模型、调整选择约束，并对每个切点给出证据链。

### 6.4 面向电影语言的人物群体连续性 Prompt

系统不把“画面里人物数量变化”简单等同于换场，而是显式处理正反打、同一人物特写、同一群体换机位等电影语言。这使 VLM 判断更接近 Scene 而非 Shot 的语义层级，属于针对影视场景的任务定义创新。

### 6.5 叙事层级与字幕连续性的结构化映射

DeepSeek 不直接对每个边界进行无上下文猜测，而是先从全片字幕构建 major/medium/minor 事件树，再把事件切换映射到 Shot 边界。该“全局规划—局部映射”机制使局部判断具有全片叙事上下文，同时在正式输出中收口为统一的字幕连续性证据。

### 6.6 缺失模态下的动态权重归一化

无对白、静音、关键帧缺失或 API 失败时，简单以 0 填充会引入系统性偏差。本方案只在实际可用的证据维度上重新归一化权重，并记录缺失原因，使系统可降级而不伪造证据。

### 6.7 整数时间、半开区间和分数 FPS 的统一时基

系统从媒体标准化、Shot 转换、字幕、关键帧到 Scene 全部采用整数毫秒和 `[start_ms,end_ms)`，FPS 使用分子/分母保存。关键帧位置和帧到毫秒的换算采用整数运算，从工程机制上降低 23.976/29.97 fps 等非整数帧率下的累积漂移。

### 6.8 单次顺序解码的关键帧抽取

系统先计算全片目标帧、全局去重、排序，再通过 PyAV 单次顺序解码命中目标。这比逐 Shot 重复打开和 seek 更适合长视频；同时结合 PTS 校验、原子写入和 SHA-256，为后续 VLM 输入提供可复现的图像证据。

### 6.9 Artifact 级可复现与细粒度重跑

每一步均落盘并生成 Manifest，模型版本、输入哈希、输出哈希、run_id 和运行时长可追溯。某一模型升级时只需使其自身及下游缓存失效，不必重新上传或处理整部视频。该机制对多模型持续接入尤为重要。

### 6.10 六层架构与七队列资源隔离

API、编排、Worker、Adapter、存储和 Schema 各司其职，CPU、GPU、API 和维护任务通过七个队列隔离。模型不接触数据库和最终输出，大文件不进入 Redis，使系统能够从单机 MVP 平滑迁移到 PostgreSQL、Redis、对象存储和多 Worker 部署。

### 6.11 从“内容可切”到“观众价值可删”的双层决策

传统自动剪辑通常把视觉变化、运动强度或人工规则直接当作删除依据；单独的 EEG 系统又常按固定窗口给分。本方案先用电影内容模型确定自然、叙事完整的可切边界，再用 EEG 判断边界内部内容是否值得保留。两个层次各自解决不同问题，降低“有低分但无法自然裁切”与“可以裁切但不应删除”的冲突。

### 6.12 四指标与群体同步的互补判定

Eng/Att/Fat/WL 描述组平均的相对心理状态，ISC 描述不同观众反应的一致程度。现有剪辑策略不把它们压成一个不可解释的黑箱分数，而是保留 `B` 与 `s1` 两条来源并取并集。编辑者能够区分“大家都不投入”和“大家反应不一致”，形成更可审查的剪辑理由。

### 6.13 质量门控的因果基线更新

低质量窗口保留输出但不参与累积 μ/σ，使后续 z-score 不会被眼动和超幅伪迹持续污染。Welford 式在线归一化只使用当前及过去的合格窗口，适合实时或顺序播放分析，减少使用未来信息造成的数据泄漏。

### 6.14 组内归一化后再跨被试聚合

不同被试的头皮阻抗、幅值尺度和个体基线不同。本方案先在被试自身 session 内形成可比较的相对指标，再按同步窗口求组平均；ISC 则采用 leave-one-out 与 Fisher-z 聚合。该机制比直接平均原始 EEG 幅值更能控制个体尺度差异。

### 6.15 Scene 约束下的 EEG 窗口吸附

EEG 指标需要较长窗口获得稳定频谱，而视频剪辑要求边界精确。本方案通过“长窗口判价值、Shot/Scene 边界定切点”解决两个时间尺度的冲突，并利用重叠率、保护区和人工复核处理跨界窗口。

### 6.16 确定性 EEG 内核与 LLM Agent 的职责分离

自然语言 Agent 只调用十个高层工具并解释结构化结果，所有 EEG 预处理、频谱、指标、ISC 和剪辑区间仍由共享算法包计算。由此既获得低门槛交互，也避免语言模型凭主观文本生成生理结论或删除时间戳。

### 6.17 从建议、复核到成片的版本化闭环

系统不仅输出 CSV 时间戳，还把建议来源、Scene/Shot 映射、用户确认、保留区间、FFmpeg 命令和成片哈希串成一条证据链。自动剪辑结果可回滚、可复算、可比较，并允许同一原片保存不同人群或不同参数产生的多个成片版本。

---

## 七、技术效果与评价方案

### 7.1 预期技术效果

1. **减少过度切分。** 对话正反打和同场换机位可因高人物/场所/字幕连续性被合并。
2. **提升边界可解释性。** 每个 Scene Boundary 可追溯到相邻 Shot、关键帧、字幕区间、连续性证据、模型版本和选择约束。
3. **降低长视频推理规模。** VLM 仅处理自然 Shot 之间的边界帧对，不处理固定间隔产生的全部时间点。
4. **提高工程稳定性。** 标准化、原子写入、哈希校验、有限重试、Manifest 和结构化日志降低长任务中断后的恢复成本。
5. **支持模型持续演进。** 新模型只需实现统一 Adapter、登记 Registry 并遵守 I/O Contract，即可替换某一证据来源。
6. **将观众反应引入片段价值判断。** 内容模型完成分段后，系统可根据 Att、Eng、Fat、WL 与 ISC 识别低投入、疲劳性动机下降或群体低同步片段。
7. **保护叙事与声画完整性。** EEG 长窗口不直接成为裁切点，而是吸附到 Scene/Shot 合法边界，并让音视频使用相同保留区间。
8. **支持研究、编辑与自然语言三类用户。** CLI、GUI 和 Agent 共用同一 `eeg_core`，分析结果和剪辑建议具有一致算法来源。
9. **形成从建议到成片的闭环。** 用户可复核建议，系统按确认结果生成剪辑工程数据和新视频版本，并保留全过程 Manifest。

以上属于基于系统机制的预期效果，不代表已经在大规模电影数据集上取得统计验证。

### 7.2 建议评价指标

| 类别 | 指标 | 定义或用途 |
|---|---|---|
| Shot 检测 | Precision / Recall / F1 | 与人工 Shot Boundary 标注比较 |
| Scene 边界 | Boundary Precision / Recall / F1 | 在允许时间容差内匹配人工 Scene Boundary |
| 场景分段 | Coverage、Overflow | 衡量预测 Scene 对真实 Scene 的覆盖与跨界程度 |
| 排序质量 | PR-AUC、Top-K Recall | 评价 `scene_score` 对真实边界的排序能力 |
| 时间准确性 | 边界时间误差（ms） | 验证整数时基与 Shot 对齐效果 |
| 效率 | 实时因子、每小时视频耗时、API 调用数 | 评估长视频吞吐和成本 |
| 资源 | CPU/GPU 峰值、内存、Artifact 大小 | 评估部署需求 |
| 稳定性 | 成功率、重试率、断点复用率 | 评估工程可靠性 |
| 可解释性 | 证据完整率、人工理由一致率 | 评价边界证据是否可审查 |
| EEG 信号质量 | 有效窗口率、伪迹比例、有效被试数 | 判断剪辑建议是否具备足够生理证据 |
| EEG 判别一致性 | 重测一致性、被试子集稳定性、阈值敏感性 | 评价五类指标建议是否稳定 |
| 人工剪辑一致性 | 建议区间 Precision/Recall/F1、IoU | 与专业编辑或盲评共识比较 |
| 叙事完整性 | 被截断对白/动作比例、边界自然度评分 | 验证 Scene/Shot 吸附的作用 |
| 成片效果 | 盲评偏好、完成率、观看保持度 | 必须通过预先定义的用户实验验证 |
| 成片正确性 | 声画同步误差、时长误差、解码成功率 | 验证 FFmpeg 裁切拼接质量 |

### 7.3 对比与消融实验

建议至少设置：

1. FFmpeg scene filter 单独分段；
2. OmniShotCut 每个 Shot 都切分；
3. 仅视觉连续性；
4. 视觉 + 场所 + 人物连续性；
5. 加入字幕连续性；
6. 加入音频连续性；
7. 无 Greedy 距离约束与有约束；
8. 320×180 与 672 px 关键帧；
9. OmniShotCut 与 FFmpeg 候选集；
10. SceneSeg/BaSSL 候选置信加入前后。
11. 仅 Engagement、仅 ISC、四指标规则、五类证据并集；
12. 单被试与多被试组平均；
13. 无 Quality 门控与 Quality≥0.7 门控；
14. EEG 原始窗口裁切与 Scene/Shot 边界吸附；
15. `Eng↓` 与 `Att↓∧WL↑∧Fat↑` 两条 B 分支的消融；
16. 低 ISC 最短持续时间 0/6/10/12 秒与不同 σ 阈值；
17. 自动决定、人工复核和纯人工剪辑三种工作流。

数据划分应以“电影”为单位，避免同一电影片段跨训练集和测试集造成信息泄漏。报告样本数、类型分布、标注规则、容差窗口、硬件、软件和模型版本，并同时给出均值与波动范围。

### 7.4 当前证据边界

- **已实现/已验证基础**：统一 Schema、FFmpeg 标准化与校验、OmniShotCut Adapter/转换/验证、关键帧整数定位与单次解码、Artifact Writer、Celery 任务骨架、测试 Fixtures。
- **已有原型代码**：Doubao ASR、Qwen2.5-VL、DeepSeek Provider 与部分 Worker；仍需端到端数据集验证、错误处理收口和正式 Schema 对齐。
- **规划中**：SceneSeg/BaSSL 选型、Visual/Audio Encoder、正式 `final.assemble`、完整 Scene 评分实现与全链路回归。
- **EEG 已实现基础**：EDF/BDF 读取、ND8/10-20 通道映射、滤波与伪迹、FFT 频段功率、Att/Eng/Fat/WL、群体 ISC、建议片段合并、GUI Edit 时间轴以及 CSV/JSON 导出。
- **EEG 与视频融合待完成**：统一毫秒同步 Schema、建议区间到 Scene/Shot 的映射、人工确认状态、FFmpeg 精确裁切拼接和成片 Manifest。
- **未提供证据**：大规模 Scene Boundary F1、真实生产吞吐、API 成本、用户偏好和商业部署效果。因此本文不写入虚构数值。

---

## 八、拟申请权利要求概述

1. 一种电影场景智能分段系统，包括视频标准化模块、Shot Boundary Detection 模块、多模态证据提取模块、Scene 候选模块、Scene Score 模块、约束选点模块、Shot 合并模块和 Artifact 存储模块。
2. 根据权利要求 1 所述的系统，其特征在于：仅在相邻自然 Shot 之间形成候选 Scene Boundary，并以边界前一个 Shot 的 `shot_id` 作为全链路唯一边界标识。
3. 根据权利要求 1 所述的系统，其特征在于：所述 Shot Boundary Detection 模块通过统一 Adapter 接入 OmniShotCut 或 FFmpeg scene filter，并将第三方输出转换为整数毫秒、半开区间的统一 Shot Schema。
4. 根据权利要求 1 所述的系统，其特征在于：对每个 Shot 通过整数比例计算 25% 与 75% 目标帧，汇总全片目标帧后进行全局去重和单次顺序解码。
5. 根据权利要求 1 所述的系统，其特征在于：视觉语言模型比较候选边界两侧关键帧，分别形成场所连续性与人物群体连续性证据，并识别对话正反打、特写和同场换机位。
6. 根据权利要求 1 所述的系统，其特征在于：文本模型先依据全片带时间戳字幕构建分层叙事事件，再将事件连续关系映射到相邻 Shot 候选边界，形成字幕连续性证据。
7. 根据权利要求 1 所述的系统，其特征在于：Scene 模型只提供候选边界或置信度，不直接决定最终切点；最终切点由候选证据、唯一 `scene_score` 与带最小时间间隔的选择算法共同确定。
8. 根据权利要求 1 所述的系统，其特征在于：当任一模态缺失时，仅在可用证据维度上重新归一化权重，并记录缺失状态与模型运行信息。
9. 根据权利要求 1 所述的系统，其特征在于：所有内部时间采用整数毫秒，时间区间采用 `[start_ms,end_ms)`，帧率以分数形式保存。
10. 根据权利要求 1 所述的系统，其特征在于：每个主要中间结果保存为版本化 Artifact，并生成包含输入输出哈希、模型版本、run_id 和时间信息的 Manifest。
11. 根据权利要求 1 所述的系统，其特征在于：API 仅创建和查询任务，耗时处理由 Celery Worker 按资源队列执行，任务间仅传递 ID、URI 或小型 JSON。
12. 一种使用上述系统进行电影场景分段的方法，包括上传与标准化、自然 Shot 检测、多模态证据提取、候选边界评分、约束选点、Shot 合并、结果装配和版本化保存步骤。
13. 根据权利要求 1 所述的系统，其特征在于：对与电影播放时间同步的前额 EEG 采用 DC 去除、0.5-45 Hz IIR 滤波、50 Hz 陷波、眼电与超幅伪迹检测、线性插值及可选重参考处理。
14. 根据权利要求 1 所述的系统，其特征在于：在滑动窗口内通过 FFT 周期图和频段功率形成 Attraction、Engagement、Fatigue 与 Workload，并通过质量阈值控制累积基线更新。
15. 根据权利要求 1 所述的系统，其特征在于：对多被试信号采用 leave-one-out 平均、逐通道 Pearson 相关和 Fisher-z 聚合形成 ISC 时间序列。
16. 根据权利要求 1 所述的系统，其特征在于：将低 Engagement，或者低 Attraction、高 Workload 和高 Fatigue 的联合状态，或者持续的极端低 ISC，标记为候选删除区间，并保留具体触发来源。
17. 根据权利要求 1 所述的系统，其特征在于：将 EEG 候选区间与 Scene 的时间重叠进行计算，并把实际裁切点吸附至 Scene Boundary 或 Shot Boundary。
18. 根据权利要求 1 所述的系统，其特征在于：经自动策略或人工复核确认删除区间后，计算其相对于完整视频时间轴的补集，裁切并按顺序拼接保留片段，输出剪辑后视频及其 Manifest。
19. 一种基于多模态影视内容与群体脑电反馈的自动剪辑方法，包括 Scene 构建、EEG 预处理、五类指标计算、片段价值判断、边界对齐、建议复核、保留区间计算、音视频裁切拼接和结果校验步骤。

> 注：正式专利申请前应由专利代理人结合现有技术检索，对独立权利要求的必要技术特征、保护范围和术语一致性进行法律层面的重构。

---

## 九、风险、限制与改进方向

1. **VLM 批次尺度漂移**：不同批次可能使用不同评分基准。需引入固定锚点、校准集和跨批归一化验证。
2. **相似布景误判**：不同地点可能视觉相似；需结合字幕、人物、音频和前后窗口，而不是只看一对帧。
3. **交叉剪辑**：影片可能在两个场所之间频繁切换但仍属于同一叙事段。可加入回看窗口、事件状态机或层次化 Scene 表示。
4. **无对白与 ASR 漂移**：无字幕时进行缺失模态降级；长视频 ASR 需检查分片边界、重复/丢词和时间戳漂移。
5. **第三方模型 License**：OmniShotCut 已记录固定 revision 与权重信息；其他模型在商业启用前仍需核验代码、权重、数据集和 API 服务条款。
6. **实验字段与正式 Schema 差异**：现有 VLM/文本实验任务需重构为正式 SceneEvidence，清理不符合长期架构约束的中间输出命名。
7. **最终装配尚未完全实现**：`final.assemble` 仍是占位实现，需按 FinalResult Schema 汇总 Video、Shot、Subtitle、Scene 与 SceneEvidence。
8. **生产安全**：正式部署需补充对象存储权限、传输与静态加密、API 密钥托管、租户隔离、审计日志和数据保留策略。
9. **EEG 与视频同步误差**：播放起点、暂停、缓冲和倍速会破坏固定偏移。必须保存播放器事件日志，并对同步残差设置验收阈值。
10. **生理指标解释边界**：Att、Eng、Fat、WL 和 ISC 是相对、间接指标，不能作为医学诊断，也不能把单一异常确定性解释为“不喜欢”。
11. **人群代表性**：少量同质被试产生的群体建议不能外推到所有受众。需报告样本量、人口属性和被试子集稳定性。
12. **阈值外推风险**：1.2σ、2.0σ、10 秒和 6 秒合并间隔来自当前策略与历史数据，需要独立验证和敏感性分析。
13. **低质量与低同步混淆**：伪迹、接触不良或丢包可能造成假性低 ISC。自动删除前应满足 Quality 和有效被试数要求。
14. **成片执行尚待接入**：当前 EEG 项目输出剪辑建议和 CSV/JSON，但尚无正式 FFmpeg 成片模块；必须在视频平台中实现并通过声画同步回归。
15. **隐私与伦理**：EEG 属于敏感生理数据，应执行知情同意、最小化采集、去标识、访问控制、保存期限和撤回机制。

---

## 十、项目实施路线

### 阶段一：单模型闭环（已具备主要基础）

- 完成视频上传、FFmpeg 标准化、OmniShotCut 检测、shots.json、Manifest、数据库记录与查询；
- 固定第三方 revision、权重路径与 SHA-256；
- 通过 Schema、时间转换、路径、缓存键和失败路径测试。

### 阶段二：VLM/字幕证据收口

- 完成 Doubao ASR 的长音频回归测试；
- 完成 Qwen2.5-VL Prompt、批次、输出校验和跨批校准；
- 将 DeepSeek 事件树转换为正式 `subtitle_continuity`；
- 清理实验字段，确保对外只输出 `scene_score`。

### 阶段三：多模态与 Scene 模型

- 选择并接入 Visual Encoder、Audio Encoder；
- 对 SceneSeg 与 BaSSL 进行 License、环境、精度和资源对比；
- 完成缺失模态、权重归一化和候选置信融合。

### 阶段四：最终装配与全链路验证

- 实现 `merge_shots_to_scenes`、`calculate_scene_score` 和 `final.assemble`；
- 覆盖 FastAPI → Celery → Redis → Adapter → Artifact → Database → API 查询集成测试；
- 使用固定测试电影执行模型、依赖、CUDA、FFmpeg 和 Schema 升级回归。

### 阶段五：性能与产品化

- 建立人工 Scene Boundary 标注集和盲评流程；
- 测量 F1、Coverage、Overflow、延迟、吞吐、资源和成本；
- 增加缓存命中率、失败恢复、模型灰度和多租户安全能力。

### 阶段六：EEG 时间轴与 Scene 融合

- 定义 `EEGRecording`、`EEGWindowMetrics`、`ISCSeries`、`EditSuggestion` 与 `EditDecision` Schema；
- 把 EEG 秒级输出统一转换为整数毫秒并保存 playback offset/event log；
- 实现建议区间与 Scene/Shot 的重叠、吸附、保护区和冲突处理；
- 把 `movie_EEG` 的确定性算法封装为 Worker/Adapter 或受控服务，不让 LLM 直接生成时间戳；
- 建立 EEG Quality、有效被试数和同步残差的执行门槛。

### 阶段七：成片执行与闭环验证

- 实现 `edit.render_video`，计算删除区间补集并调用 FFmpeg 精确裁切、拼接；
- 输出 MP4、EDL、JSON/CSV、用户确认记录和 Artifact Manifest；
- 进行声画同步、时长、编解码、空区间、相邻区间和回滚测试；
- 对比人工剪辑、仅内容模型、仅 EEG 和融合系统，开展盲评、重测与阈值敏感性实验；
- 根据验证结果固化默认参数，并保留针对人群、片型和应用目标的可配置策略。

---

## 十一、附图说明（建议）

1. **图 1：六层系统总体架构图**——展示 API、编排、Worker、Adapter、存储和 Schema 的依赖方向。
2. **图 2：多模型视频分析数据流图**——展示从原始视频到 final_result.json 的 Artifact 链路。
3. **图 3：Shot 与 Scene 层级关系图**——说明多个 Shot 合并为一个 Scene，以及 `shot_id` 如何标识候选边界。
4. **图 4：关键帧单次解码流程图**——展示目标帧计算、全局去重、顺序解码、PTS 校验和原子写入。
5. **图 5：多模态 SceneEvidence 融合图**——展示视觉、人物、场所、字幕、音频与时间证据如何形成唯一 `scene_score`。
6. **图 6：约束选点示意图**——展示候选排序、最小间隔过滤、目标数量限制和时间顺序恢复。
7. **图 7：Artifact 与 Manifest 追溯图**——展示 project_id、video_id、model、version、run_id 和 SHA-256 的关系。
8. **图 8：硬件拓扑图**——沿用参考文档中的 EEG、PPG、IMU、环境光传感器、相机和 RK3588 连接关系。
9. **图 9：EEG 预处理与四指标流程图**——展示 V→μV、滤波、伪迹、Hanning-FFT、频段功率和 Att/Eng/Fat/WL。
10. **图 10：leave-one-out 群体 ISC 图**——展示目标被试、其余被试平均、Pearson 相关和 Fisher-z 聚合。
11. **图 11：五类证据剪辑规则图**——展示 `s1`、`B`、并集、时间合并和低 ISC 最短时长过滤。
12. **图 12：EEG 窗口与 Scene/Shot 边界对齐图**——展示长 EEG 窗口如何通过重叠率吸附到合法剪辑边界。
13. **图 13：建议—复核—成片闭环图**——展示 GUI/Agent 复核、删除集合、保留补集、FFmpeg 拼接与 Manifest。

---

## 十二、证据与成熟度登记

| 论述 | 状态 | 项目依据 | 限制 |
|---|---|---|---|
| 六层架构、七队列、API 不做长推理 | 已验证设计 | `AGENTS.md`、README、Celery 配置与目录结构 | 仍需生产负载验证 |
| 整数毫秒、半开区间、分数 FPS | 已实现基础 | `IO_Rule.md`、Shot/Scene Schema、媒体与转换代码 | 所有新 Adapter 仍需持续校验 |
| OmniShotCut 单模型闭环 | 已实现/已有本地验证记录 | Adapter、Converter、Validation、Fixtures、集成测试与本地验证报告 | Registry 当前仍为 `enabled: false`，生产启用需环境确认 |
| FFmpeg 轻量 Shot 备选 | 已有代码 | `models/ffmpeg_scene/adapter.py` | 需在统一数据集上与主模型对比 |
| 25%/75% 关键帧与单次解码 | 已实现代码 | `core/media/keyframes.py` | 需评估极短 Shot 和解码异常 |
| Doubao ASR 长音频分片 | 原型代码 | WhisperAdapter、Doubao Provider、subtitle Worker | 需真实长片时间戳回归 |
| Qwen2.5-VL 场所/人物判断 | 原型代码与方案 | VLM Adapter、Provider、Prompt、计划文档 | 模型当前未启用，需校准与数据集验证 |
| DeepSeek 分层事件理解 | 原型代码与方案 | LLM Adapter、Provider、Worker | 正式 Schema 映射仍需收口 |
| SceneSeg/BaSSL | 规划中 | Registry 与 scene boundary README | 尚未选型、未实现生产 Adapter |
| Visual/Audio Encoder | Contract/规划中 | `IO_Rule.md`、Registry | 尚无完整生产闭环 |
| Scene 评分与 final.assemble | 部分规划/占位 | Scene Schema、scene/final tasks | 正式实现与回归仍待完成 |
| BDF/EDF 读取与通道映射 | 已实现代码 | `eeg_core/io`、`channel_mapping`、`montage.txt` | 不同设备映射仍需逐型号验证 |
| EEG 滤波、伪迹与 Quality | 已实现代码 | `preprocessing`、`artefact_detection`、集中配置 | 插值对频谱影响需对照实验 |
| Att/Eng/Fat/WL 四指标 | 已实现代码 | `eeg_core/scoring`、GUI/CLI/Agent 调用 | 属 session 相对指标，非医学诊断量表 |
| 多被试群体 ISC | 已实现代码 | `eeg_core/sync_rate`、ISC Worker | 少样本与同步误差敏感性需验证 |
| 五类证据剪辑建议 | 已实现原型代码 | `edit_suggestion`、`STRATEGY.md`、GUI Edit、Agent 工具 | 默认阈值的跨电影泛化尚未验证 |
| CLI + 五 Tab GUI + LLM Agent | 已实现代码 | `apps/cli`、`apps/gui`、`apps/agent` | Agent 解释质量依赖模型，时间戳必须来自工具 |
| 建议区间到 Scene/Shot 的正式映射 | 融合规划中 | 两项目时间结构与本文接口设计 | 尚需 Schema、实现与回归 |
| 剪辑完成后视频输出 | 规划中 | Movie Analysis Platform FFmpeg/Artifact 能力可复用 | 当前 EEG 项目仅导出建议，未形成正式成片模块 |
| 大规模精度、吞吐和成本优势 | 未验证 | 尚无完整评测数据 | 必须通过对比与消融实验取得 |

---

## 十三、注意事项

1. 本技术方案包含项目架构、模型组织和数据处理细节，对外披露前应根据知识产权策略控制范围。
2. 正式申请专利前，应检索 Shot Boundary Detection、Scene Segmentation、VLM 视频理解、Artifact 工作流等相关现有技术，避免把通用工程实践单独主张为发明点。
3. 创新性应优先落在“Shot 候选空间压缩 + 多模态连续性证据 + 模型与选择算法职责分离 + 全链路统一边界 ID + 可复现 Artifact”这一组合机制上。
4. 硬件部分为参考文档原有方案，若本项目最终仅申请后端软件/方法专利，应由代理人判断是否保留为从属实施例，避免独立权利要求主题不统一。
5. 在获得真实评测数据前，不应宣称具体准确率、成本降低比例、生产吞吐或用户偏好提升。
6. EEG 指标和剪辑建议不得用于疾病诊断、心理状态定性或对个体作高风险决策；项目定位为受控观看实验和视频编辑辅助。
7. 多被试数据必须取得知情同意并进行去标识化；向 LLM 或外部服务发送数据时，应只发送完成任务所需的最小结构化摘要，禁止上传原始 EEG。
8. 自动成片应默认保留人工复核开关，并提供原片、决策清单和可逆版本；未经确认不得覆盖原始视频。
9. 发明保护重点可进一步凝练为“多模态 Scene 构建—群体 EEG 价值判断—跨时间尺度边界对齐—可追溯成片执行”的闭环组合。
