# 多模态电影场景智能分段系统技术方案

> 本文为当前项目的合并架构说明，取代旧“项目图纸”和历史实施计划。内容以 `master` 的 FastAPI、Workflow、Adapter、Schema 与数据库模型为准。

## 1. 目标与边界

系统把一个上传的视频拆分为 Shot，并以地点、人物和字幕语义连续性为证据，把连续 Shot 合并为 Scene。输出只包含 Scene 层的 `scene_score`，并保留所有中间产物、模型运行和数据库血缘，以便查询、下载、复现、缓存和重试。

本阶段采用单进程部署内的 FastAPI + Python Workflow/Executor。它不提供 Celery/Redis 的跨机器调度或进程退出后继续运行保证；应用启动会将遗留运行任务标为 `INTERRUPTED`。

## 2. 分层与职责

| 层 | 位置 | 职责 |
|---|---|---|
| API | `apps/api/routes/` | 上传、校验、创建任务、状态与结果查询；不执行长推理 |
| 执行/编排 | `apps/api/services/workflow_service.py`、`executor.py` | 调度步骤、更新进度、缓存、失败处理、血缘记录 |
| Adapter | `models/*/adapter.py` | Provider 或模型调用与输出标准化 |
| 存储 | `core/task_storage.py`、`core/artifacts/`、`core/database/` | 文件、Manifest、签名 URL、PostgreSQL/SQLite |
| Schema | `schemas/` | 跨层数据结构和数值/时间约束 |

`BackgroundExecutor` 使用受控线程池（当前 API 创建时为 2 个 worker）。路由只提交 Workflow；大型媒体和数组从不作为线程参数在步骤间复制。

## 3. 端到端数据流

```text
POST /projects/{project_id}/videos
  -> 流式写原文件，SHA-256 + FFprobe 校验
  -> Video / Project 记录，返回 video_id

POST /videos/{video_id}/tasks
  -> Task + WorkflowRun，返回 task_id
  -> BackgroundExecutor 提交 Workflow

video.normalize
  -> video.normalized + audio.normalized + video_probe
shot.detect -> shots
shot.extract_keyframes -> shot_keyframes
audio.transcribe -> subtitle_segments
subtitle.semantic_continuity -> subtitle_continuity
scene.score_visual_continuity -> location_character_scores
scene.merge_score -> CandidateBoundary、Scene、SceneEvidence、final_scenes
```

ASR 使用 Provider Gateway 和 ngrok/Tunnel 公共地址获取短期签名的 `audio.normalized` 下载 URL。Vision 使用关键帧和 Shot 描述生成地点、人物连续性。字幕语义步骤先生成全片与局部叙事候选，再映射到 Shot 边界，输出 `subtitle_continuity`。

## 4. 场景边界与评分

每个非末尾 Shot 都可成为候选边界。连续性越低，变化越大。系统按选择模式取可用的地点、人物或字幕连续性，按权重归一化为 `[0,1]` 的 `scene_score`；随后按分数降序优先选择高分边界，应用最小距离约束并达到目标数量后停止。所选边界将 Shot 合并成连续 Scene。

| 模式 | 有效权重 |
|---|---|
| `location_only` | location=1 |
| `character_only` | character=1 |
| `subtitle_only` | subtitle=1 |
| `custom` | 三个请求权重按总和归一化，禁止全零 |

不使用 `action_score`、`plot_score` 或对应 Evidence。视觉模型只生成证据，不能直接决定最终切点。

## 5. 数据与 Artifact

原视频只保存一次。主要路径以 project、video、task、模型和版本隔离；数据库保存元数据和血缘，不保存视频内容。核心关系为：

```text
Video -> Task -> WorkflowRun / ModelRun -> Artifact
                                      -> ModelRunInput / ModelRunOutput
Task + producer ModelRun -> Shot / CandidateBoundary / Scene / SceneEvidence
```

主要 Artifact 均有 Manifest、SHA-256、大小和 `storage://` URI。跨任务缓存只复用验证成功的结果；任务级 Shot、Subtitle 和 Scene ID 会重新映射，因此不会与旧任务冲突。

## 6. API、恢复与可观测性

API 路径、请求参数与下载方式见 [README.md](README.md)。`GET /health/live` 只检查进程；`GET /health/ready` 检查数据库、存储、FFmpeg、Provider Gateway、公共 URL 和 Provider 配置。任务状态为 `PENDING`、`QUEUED`、`RUNNING`、`SUCCEEDED`、`FAILED`、`INTERRUPTED` 等；失败包含 error code、消息和 retryable 标志。

运行时间记录在 ModelRun；Workflow 将阶段和进度写入 Task。日志必须包含 task、video、run 和模型上下文。

## 7. 开发与演进规则

- 新模型先定义 Schema/Contract，再实现独立 Adapter 并在 `models/registry.yaml` 登记。
- 新 ORM 字段必须附带可升级、可回滚的 Alembic migration。
- 修改后至少执行 Ruff、类型检查和单元/集成测试；固定测试视频可用于回归。
- 如需 Celery、Redis、跨机器调度或持久队列，必须作为新的架构变更评审，不能与当前进程内执行混用。

详细硬性规则见 [AGENTS.md](AGENTS.md)，输入输出细节见 [IO_Rule.md](IO_Rule.md)。
