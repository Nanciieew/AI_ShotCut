# ============================================================
# Movie Analysis Platform — Dependency Entry Point
# ============================================================
# For development (API + Worker + tools):
#   pip install -r requirements.txt
#
# For production API only:
#   pip install -r requirements/api.txt
#
# For production Worker only:
#   pip install -r requirements/worker.txt
#
# Individual model dependencies are in requirements/models/

-r requirements/dev.txt
