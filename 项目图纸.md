# 项目图纸

> 本文档由以下两份原始文档合并而成，内容未做任何修改：  
> 1. 多模型视频分析后端架构与实施规范  
> 2. 影片自动分段算法与数据流程设计

---

# 第一部分：后端架构与实施规范

# 多模型视频分析后端架构与实施规范

> 适用项目：电影镜头切分、场景合并、多模态特征分析与 Scene Score 计算  
> 目标读者：负责实现后端、接入 GitHub 模型、维护数据流与任务系统的开发 Agent  
> 当前阶段：MVP  
> 核心技术栈：FastAPI + Celery + Redis + PostgreSQL/SQLite + FFmpeg + Python 模型适配器

---

# 1. 项目目标

本项目需要构建一个支持持续接入多个 AI 模型的视频分析后端。

系统需要完成以下核心流程：

```text
上传电影
→ 视频标准化
→ 镜头检测
→ 字幕生成
→ 多模态特征提取
→ 场景边界判断
→ Shot 合并为 Scene
→ 计算 Scene Score
→ 保存并返回最终结果
```

当前明确规则：

1. OmniShotCut 只负责 Shot Boundary Detection。
2. Scene 模型负责判断相邻 Shot 是否属于同一 Scene。
3. 系统只保留一个最终评分概念：`scene_score`。
4. 系统中不允许出现：
   - `action_score`
   - `plot_score`
   - action evidence
   - plot evidence
5. 各模型之间不能直接调用。
6. 所有模型必须由统一任务编排层调度。
7. 所有模型必须通过 Adapter 接入。
8. 所有耗时任务从 MVP 开始就使用 Celery。
9. Redis 作为 Celery Broker 和必要的任务状态中间层。
10. 所有中间产物必须落盘或进入对象存储，不能只保存在内存中。

---

# 2. MVP 技术架构

MVP 不使用 FastAPI BackgroundTasks。

从第一版开始采用：

```text
Frontend
   ↓ HTTP
FastAPI
   ↓ 创建 Task
Redis
   ↓ 分发任务
Celery Worker
   ↓ 调用模型
Artifact Storage
   ↓
Database
   ↓
Frontend 查询任务状态与结果
```

推荐 MVP 技术栈：

```text
API：FastAPI
任务队列：Celery
消息代理：Redis
数据库：PostgreSQL
本地开发数据库备选：SQLite
视频处理：FFmpeg
模型运行：PyTorch / Python
文件存储：本地 data 目录
正式部署存储：S3 / MinIO / OSS
容器：Docker Compose
```

MVP 即应支持：

- 上传视频；
- 创建异步任务；
- 返回 `task_id`；
- 查询任务状态；
- 查询任务进度；
- 获取模型结果；
- 失败记录；
- 任务重试；
- 中间结果复用；
- 模型版本追踪。

---

# 3. 系统分层

系统必须分为六层。

## 3.1 API 层

负责：

- 接收前端请求；
- 校验输入；
- 上传文件；
- 创建任务；
- 返回 `task_id`；
- 查询任务状态；
- 返回最终结果；
- 不直接执行模型推理。

API 层禁止：

- 直接加载模型；
- 直接运行长时间 FFmpeg；
- 直接执行 Whisper；
- 直接执行 OmniShotCut；
- 在 HTTP 请求中等待整部电影处理完成。

---

## 3.2 编排层 Orchestration

负责：

- 定义任务执行顺序；
- 决定哪些步骤并行；
- 决定哪些步骤等待；
- 读取前置 Artifact；
- 调用对应 Celery Task；
- 记录 Pipeline 状态；
- 处理失败；
- 跳过已经完成的步骤；
- 触发最终结果组装。

模型不能决定下一个模型是谁。

模型之间不能相互 import 和调用。

---

## 3.3 Worker 层

Celery Worker 负责：

- 执行 FFmpeg 预处理；
- 执行模型推理；
- 保存 Artifact；
- 更新任务进度；
- 记录日志；
- 处理重试；
- 返回结构化运行结果。

建议未来按任务类型拆 Worker：

```text
worker-video
worker-omnishotcut
worker-whisper
worker-feature
worker-scene
```

MVP 可以先使用一个 Celery Worker，但代码结构必须允许后续拆分。

---

## 3.4 模型适配层 Model Adapter

所有外部模型必须通过 Adapter 接入。

上层代码不能直接调用第三方模型原始 API。

统一接口：

```python
from abc import ABC, abstractmethod
from typing import Any


class BaseModelAdapter(ABC):
    name: str
    version: str

    @abstractmethod
    def load(self) -> None:
        """加载模型与权重。"""

    @abstractmethod
    def predict(self, model_input: dict[str, Any]) -> dict[str, Any]:
        """执行推理并返回统一格式结果。"""

    @abstractmethod
    def health_check(self) -> bool:
        """检查模型是否可用。"""

    @abstractmethod
    def unload(self) -> None:
        """释放模型资源。"""
```

每个模型 Adapter 必须负责：

- 读取统一输入；
- 转换成模型原始输入；
- 调用原始模型；
- 将原始输出转换成统一 Schema；
- 捕获模型异常；
- 记录模型版本；
- 返回 Artifact 信息。

---

## 3.5 存储层

存储分为两类。

### 文件与 Artifact 存储

保存：

- 原始视频；
- 标准化视频；
- 音频；
- 截帧；
- 模型权重；
- JSON 结果；
- 特征矩阵；
- 导出短片；
- 日志文件。

### 数据库

保存：

- 用户；
- 项目；
- 视频元数据；
- Task；
- Model Run；
- Artifact 索引；
- Scene；
- Scene Score；
- 状态与错误信息。

数据库不保存整部视频。

---

## 3.6 Schema 层

所有跨模块数据必须遵循统一 Schema。

不得让某个模型随意定义自己的字段命名并直接传给下一个模型。

统一 Schema 需要覆盖：

- Video
- Task
- Model Run
- Artifact
- Shot
- Subtitle Segment
- Scene
- Scene Evidence
- Final Result

---

# 4. 多模型数据流

完整数据传输路径：

```text
原始视频 source.mp4
    ↓
上传并生成 video_id
    ↓
创建 pipeline_task_id
    ↓
Celery：normalize_video
    ↓
normalized.mp4
audio.wav
video_metadata.json
    ↓
并行启动
    ├── Celery：detect_shots
    ├── Celery：transcribe
    └── Celery：extract_audio_features
    ↓
shots.json
subtitles.json
audio_features.npy
    ↓
Celery：extract_visual_features
    ↓
visual_features.npy
    ↓
Celery：build_scene_features
    ↓
scene_features.npz
    ↓
Celery：detect_scene_boundaries
    ↓
scene_boundaries.json
    ↓
Celery：merge_shots_to_scenes
    ↓
scenes.json
    ↓
Celery：calculate_scene_score
    ↓
scene_scores.json
    ↓
Celery：assemble_final_result
    ↓
final_result.json
```

---

# 5. Celery Pipeline 设计

推荐使用 Celery Canvas：

- `chain`
- `group`
- `chord`

示意：

```python
from celery import chain, group, chord

pipeline = chain(
    normalize_video.s(video_id),
    chord(
        group(
            detect_shots.s(),
            transcribe.s(),
            extract_audio_features.s(),
        ),
        collect_parallel_results.s(),
    ),
    extract_visual_features.s(),
    build_scene_features.s(),
    detect_scene_boundaries.s(),
    merge_shots_to_scenes.s(),
    calculate_scene_score.s(),
    assemble_final_result.s(),
)
```

注意：

1. 不要在 Celery Task 之间直接传输大型数组。
2. Task 之间只传：
   - ID；
   - URI；
   - 少量 JSON；
   - 状态信息。
3. 大型数据必须先保存为 Artifact。
4. 下一个 Task 通过 Artifact URI 读取。
5. Celery Result Backend 不作为长期 Artifact 存储。
6. Redis 不保存整部视频或大型特征矩阵。

---

# 6. 时间坐标统一规则

系统内部统一使用：

```text
整数毫秒
```

字段：

```json
{
  "start_ms": 12540,
  "end_ms": 17820
}
```

时间区间统一为：

```text
[start_ms, end_ms)
```

含义：

- 包含 `start_ms`；
- 不包含 `end_ms`。

示例：

```text
shot_1 = [0, 4000)
shot_2 = [4000, 7200)
```

禁止将浮点秒作为唯一时间依据。

可以额外保存：

- `start_frame`
- `end_frame_exclusive`
- `fps_num`
- `fps_den`
- 可读时间码

FPS 推荐保存为分数：

```json
{
  "fps_num": 24000,
  "fps_den": 1001
}
```

---

# 7. 核心数据 Schema

## 7.1 Video

```json
{
  "video_id": "video_001",
  "project_id": "project_001",
  "source_uri": "storage://projects/project_001/videos/video_001/source/original.mp4",
  "normalized_uri": "storage://projects/project_001/videos/video_001/normalized/video.mp4",
  "audio_uri": "storage://projects/project_001/videos/video_001/normalized/audio.wav",
  "duration_ms": 7200340,
  "fps_num": 24000,
  "fps_den": 1001,
  "width": 1920,
  "height": 1080,
  "audio_sample_rate": 16000
}
```

---

## 7.2 Task

```json
{
  "task_id": "task_001",
  "video_id": "video_001",
  "task_type": "full_video_analysis",
  "status": "RUNNING",
  "stage": "detect_shots",
  "progress": 25,
  "created_at": "2026-07-27T10:00:00+09:00",
  "started_at": "2026-07-27T10:00:10+09:00",
  "finished_at": null,
  "error": null
}
```

允许状态：

```text
PENDING
QUEUED
RUNNING
SUCCEEDED
FAILED
CANCELLED
SKIPPED
RETRYING
```

---

## 7.3 Model Run

```json
{
  "run_id": "run_001",
  "task_id": "task_001",
  "video_id": "video_001",
  "model_name": "omnishotcut",
  "model_version": "1.0.0",
  "code_revision": "a1b2c3d4",
  "weight_revision": "checkpoint_xyz",
  "schema_version": "1.0",
  "parameters": {
    "mode": "clean_shot"
  },
  "status": "SUCCEEDED",
  "runtime_ms": 162400,
  "device": "cuda:0"
}
```

---

## 7.4 Artifact

```json
{
  "artifact_id": "artifact_001",
  "video_id": "video_001",
  "run_id": "run_001",
  "artifact_type": "shot_boundaries",
  "uri": "storage://projects/project_001/videos/video_001/artifacts/omnishotcut/1.0.0/shots.json",
  "format": "json",
  "schema_version": "1.0",
  "sha256": "完整文件哈希",
  "created_at": "2026-07-27T10:12:00+09:00"
}
```

---

## 7.5 Shot

```json
{
  "shot_id": "shot_000001",
  "video_id": "video_001",
  "index": 0,
  "start_ms": 0,
  "end_ms": 4280,
  "start_frame": 0,
  "end_frame_exclusive": 103,
  "boundary_type": "hard_cut",
  "confidence": 0.94
}
```

---

## 7.6 Subtitle Segment

```json
{
  "subtitle_id": "subtitle_000001",
  "video_id": "video_001",
  "start_ms": 1260,
  "end_ms": 3480,
  "text": "我们必须马上离开。",
  "language": "zh",
  "confidence": 0.91
}
```

---

## 7.7 Scene

```json
{
  "scene_id": "scene_000001",
  "video_id": "video_001",
  "index": 0,
  "start_ms": 0,
  "end_ms": 68420,
  "shot_ids": [
    "shot_000001",
    "shot_000002",
    "shot_000003"
  ],
  "boundary_confidence": 0.88,
  "scene_score": 0.82
}
```

---

## 7.8 Scene Evidence

允许的 Evidence：

```json
{
  "scene_id": "scene_000001",
  "evidence": {
    "visual_continuity": 0.91,
    "character_continuity": 0.84,
    "location_continuity": 0.87,
    "subtitle_continuity": 0.79,
    "audio_continuity": 0.75,
    "temporal_gap_ms": 0
  }
}
```

禁止字段：

```text
action_score
plot_score
action_evidence
plot_evidence
```

---

# 8. 模型输入输出 Contract

所有模型 Task 输入使用统一外壳。

## 输入

```json
{
  "schema_version": "1.0",
  "task_id": "task_001",
  "video_id": "video_001",
  "model": {
    "name": "omnishotcut",
    "version": "1.0.0"
  },
  "input": {
    "video_uri": "storage://projects/project_001/videos/video_001/normalized/video.mp4"
  },
  "parameters": {
    "mode": "clean_shot"
  }
}
```

## 成功输出

```json
{
  "schema_version": "1.0",
  "task_id": "task_001",
  "video_id": "video_001",
  "status": "SUCCEEDED",
  "model": {
    "name": "omnishotcut",
    "version": "1.0.0"
  },
  "artifacts": {
    "shots": "storage://projects/project_001/videos/video_001/artifacts/omnishotcut/1.0.0/shots.json"
  },
  "metrics": {
    "shot_count": 842,
    "runtime_ms": 162400
  },
  "error": null
}
```

## 失败输出

```json
{
  "schema_version": "1.0",
  "task_id": "task_001",
  "video_id": "video_001",
  "status": "FAILED",
  "model": {
    "name": "omnishotcut",
    "version": "1.0.0"
  },
  "artifacts": {},
  "metrics": {},
  "error": {
    "code": "VIDEO_DECODE_FAILED",
    "message": "FFmpeg could not decode the input video.",
    "retryable": false
  }
}
```

---

# 9. 项目目录规范

```text
movie-analysis-platform/
│
├── apps/
│   └── api/
│       ├── main.py
│       ├── dependencies.py
│       ├── routes/
│       │   ├── videos.py
│       │   ├── tasks.py
│       │   └── results.py
│       ├── schemas/
│       └── services/
│
├── core/
│   ├── orchestration/
│   │   ├── pipeline.py
│   │   ├── canvas.py
│   │   └── progress.py
│   ├── storage/
│   │   ├── base.py
│   │   ├── local.py
│   │   └── s3.py
│   ├── database/
│   │   ├── session.py
│   │   ├── models.py
│   │   └── repositories/
│   ├── logging/
│   ├── hashing/
│   └── exceptions/
│
├── models/
│   ├── base/
│   │   └── adapter.py
│   ├── omnishotcut/
│   │   ├── adapter.py
│   │   ├── config.yaml
│   │   ├── requirements.txt
│   │   ├── README.md
│   │   └── tests/
│   ├── whisper/
│   │   ├── adapter.py
│   │   ├── config.yaml
│   │   ├── requirements.txt
│   │   ├── README.md
│   │   └── tests/
│   └── scene_boundary/
│       ├── adapter.py
│       ├── config.yaml
│       ├── requirements.txt
│       ├── README.md
│       └── tests/
│
├── workers/
│   ├── celery_app.py
│   ├── signals.py
│   └── tasks/
│       ├── video_tasks.py
│       ├── shot_tasks.py
│       ├── subtitle_tasks.py
│       ├── feature_tasks.py
│       ├── scene_tasks.py
│       └── final_tasks.py
│
├── pipelines/
│   ├── full_video_analysis.py
│   └── definitions/
│
├── schemas/
│   ├── video.py
│   ├── task.py
│   ├── model_run.py
│   ├── artifact.py
│   ├── shot.py
│   ├── subtitle.py
│   ├── scene.py
│   └── result.py
│
├── third_party/
│   └── README.md
│
├── configs/
│   ├── development.yaml
│   ├── production.yaml
│   ├── models.yaml
│   └── celery.yaml
│
├── data/
│   └── projects/
│
├── model_store/
│   └── README.md
│
├── scripts/
│   ├── download_models.py
│   ├── check_environment.py
│   ├── initialize_database.py
│   └── run_demo_pipeline.py
│
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
│
├── docker/
│   ├── api.Dockerfile
│   └── worker.Dockerfile
│
├── docker-compose.yml
├── pyproject.toml
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

---

# 10. 文件存储路径规范

开发阶段：

```text
data/
└── projects/
    └── project_001/
        └── videos/
            └── video_001/
                ├── source/
                │   └── original.mp4
                ├── normalized/
                │   ├── video.mp4
                │   ├── audio.wav
                │   └── metadata.json
                ├── artifacts/
                │   ├── omnishotcut/
                │   │   └── 1.0.0/
                │   │       └── shots.json
                │   ├── whisper/
                │   │   └── large-v3/
                │   │       └── subtitles.json
                │   ├── visual_encoder/
                │   │   └── 1.0.0/
                │   │       └── embeddings.npy
                │   ├── audio_encoder/
                │   │   └── 1.0.0/
                │   │       └── embeddings.npy
                │   └── scene_boundary/
                │       └── 0.1.0/
                │           └── boundaries.json
                ├── scenes/
                │   ├── scenes.json
                │   └── scene_scores.json
                └── final/
                    └── final_result.json
```

路径必须包含：

- `project_id`
- `video_id`
- 模型名称
- 模型版本

禁止使用：

```text
results/output.json
results/final.json
data/test.mp4
```

这类无法追踪来源的命名。

---

# 11. 第三方 GitHub 模型管理规则

第三方模型代码与自有业务代码必须分离。

优先级：

## 方案 1：固定 Commit 安装

```text
pip install git+https://github.com/org/repo.git@COMMIT_HASH
```

适合：

- 仓库可作为 Python 包安装；
- 不需要修改内部源码；
- 官方仓库维护正常。

必须固定 Commit，不允许生产环境直接跟踪 `main` 或 `master`。

---

## 方案 2：Git Submodule

目录：

```text
third_party/
└── OmniShotCut/
```

适合：

- 仓库不能直接安装；
- 需要读取源码；
- 需要保留原仓库版本关系。

---

## 方案 3：复制源码

默认禁止。

仅在以下条件同时满足时允许：

- 原仓库无法正常集成；
- 必须修改源码；
- License 允许；
- 修改内容被完整记录；
- 保留原始仓库地址；
- 保留原始 Commit；
- 提供 `PATCHES.md`。

---

# 12. 模型权重管理规则

模型权重禁止提交到 GitHub。

权重存储：

```text
model_store/
├── omnishotcut/
│   └── 1.0.0/
│       ├── model.pth
│       └── metadata.json
├── whisper/
│   └── large-v3/
└── scene_boundary/
    └── 0.1.0/
        ├── model.ckpt
        └── metadata.json
```

`metadata.json`：

```json
{
  "model_name": "omnishotcut",
  "model_version": "1.0.0",
  "source_repository": "官方仓库地址",
  "source_revision": "固定 Commit",
  "weight_file": "model.pth",
  "sha256": "完整校验值",
  "license": "必须人工核验",
  "downloaded_at": "2026-07-27"
}
```

GitHub 保存：

- 下载脚本；
- 配置；
- SHA256；
- 版本说明；
- License 说明。

GitHub 不保存：

- 模型权重；
- 用户电影；
- 用户数据；
- 大型特征；
- `.env`；
- API Key；
- 数据库文件。

---

# 13. 模型依赖与运行环境

MVP 可以先共用一个 Worker Python 环境，但必须预留拆分能力。

推荐顺序：

## 阶段 1

```text
api + celery worker + 多模型
共享一个 Python 环境
```

## 阶段 2

当出现依赖冲突时：

```text
api 环境
omnishotcut worker 环境
whisper worker 环境
scene worker 环境
```

## 阶段 3

使用多个 Docker Image：

```text
api container
redis container
postgres container
worker-video container
worker-omnishotcut container
worker-whisper container
worker-scene container
```

拆分条件：

- Python 版本冲突；
- PyTorch 版本冲突；
- CUDA 版本冲突；
- 显存需求差异；
- 某模型需要单独扩容；
- 某模型运行时间明显更长；
- 某模型由独立团队维护。

原则：

```text
先模块化
后服务化
```

---

# 14. 配置管理

禁止写死：

```python
MODEL_PATH = "/Users/name/Desktop/model.pth"
VIDEO_PATH = "/Users/name/Desktop/movie.mp4"
DEVICE = "cuda:0"
```

统一使用配置文件和环境变量。

`configs/models.yaml`：

```yaml
models:
  omnishotcut:
    enabled: true
    adapter: models.omnishotcut.adapter.OmniShotCutAdapter
    version: "1.0.0"
    device: "cuda:0"
    weights_uri: "model-store://omnishotcut/1.0.0/model.pth"
    parameters:
      mode: "clean_shot"

  whisper:
    enabled: true
    adapter: models.whisper.adapter.WhisperAdapter
    version: "large-v3"
    device: "cuda:0"
    parameters:
      word_timestamps: true
```

`.env.example`：

```text
DATABASE_URL=
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
STORAGE_ROOT=/data
MODEL_STORE_ROOT=/models
```

`.env` 必须加入 `.gitignore`。

---

# 15. Celery 配置规则

建议配置：

```python
task_track_started = True
task_acks_late = True
worker_prefetch_multiplier = 1
task_reject_on_worker_lost = True
broker_connection_retry_on_startup = True
```

长任务建议：

- `acks_late=True`
- `prefetch_multiplier=1`
- 明确软超时与硬超时
- 使用有限重试
- 保存当前 Stage
- Worker 丢失时允许任务重新入队

不要对以下错误无限重试：

- 视频损坏；
- 格式不支持；
- Schema 错误；
- 权重不兼容；
- CUDA 环境错误；
- 模型代码错误。

可以重试：

- 临时网络错误；
- 对象存储超时；
- Redis 短暂不可用；
- Worker 临时退出；
- 模型下载超时。

---

# 16. 幂等性规则

每个 Task 必须尽量幂等。

同一个 Task 重复执行时：

- 不得产生冲突 ID；
- 不得覆盖其他版本 Artifact；
- 不得重复插入相同 Scene；
- 已成功的结果应允许复用；
- 写文件应采用临时文件 + 原子替换；
- 数据库写入应支持 Upsert 或唯一约束。

Artifact 写入建议：

```text
shots.json.tmp
→ 校验完成
→ rename 为 shots.json
```

---

# 17. 缓存规则

每一步生成 Cache Key：

```text
cache_key =
输入文件 SHA256
+ 模型名称
+ 模型版本
+ 模型参数
+ Schema 版本
+ 代码版本
```

若完全一致且已有成功 Artifact：

```text
Task 状态设为 SKIPPED
复用已有 Artifact
```

更换以下任一项必须重新计算：

- 视频内容；
- 模型权重；
- 模型版本；
- 参数；
- Schema；
- 关键代码。

---

# 18. 日志规范

禁止只使用普通 `print()`。

每条日志至少包含：

```json
{
  "timestamp": "2026-07-27T10:00:00+09:00",
  "level": "INFO",
  "task_id": "task_001",
  "video_id": "video_001",
  "run_id": "run_001",
  "model": "omnishotcut",
  "event": "inference_started"
}
```

完成日志：

```json
{
  "level": "INFO",
  "task_id": "task_001",
  "video_id": "video_001",
  "model": "omnishotcut",
  "event": "inference_completed",
  "runtime_ms": 162400,
  "shot_count": 842
}
```

失败日志：

```json
{
  "level": "ERROR",
  "task_id": "task_001",
  "video_id": "video_001",
  "model": "omnishotcut",
  "event": "inference_failed",
  "error_code": "CUDA_OUT_OF_MEMORY",
  "retryable": true
}
```

---

# 19. API 设计

## 上传视频

```http
POST /api/v1/videos
```

返回：

```json
{
  "video_id": "video_001",
  "upload_status": "SUCCEEDED"
}
```

---

## 启动分析

```http
POST /api/v1/videos/video_001/analysis
```

返回：

```json
{
  "task_id": "task_001",
  "video_id": "video_001",
  "status": "QUEUED"
}
```

---

## 查询任务

```http
GET /api/v1/tasks/task_001
```

返回：

```json
{
  "task_id": "task_001",
  "status": "RUNNING",
  "stage": "detect_shots",
  "progress": 25
}
```

---

## 查询结果

```http
GET /api/v1/videos/video_001/results
```

当未完成：

```json
{
  "status": "PROCESSING"
}
```

完成后：

```json
{
  "status": "SUCCEEDED",
  "result_uri": "storage://projects/project_001/videos/video_001/final/final_result.json"
}
```

---

# 20. OmniShotCut 接入规则

OmniShotCut 定位：

```text
Shot Boundary Detection
```

它只负责：

- 镜头边界；
- Shot 范围；
- 可选的转场类型；
- 可选的置信度。

它不负责：

- Scene 合并；
- Scene Score；
- 情节理解；
- 动作评分；
- 剧情评分。

标准输出必须转换成：

```json
{
  "video_id": "video_001",
  "model": {
    "name": "omnishotcut",
    "version": "1.0.0"
  },
  "shots": [
    {
      "shot_id": "shot_000001",
      "index": 0,
      "start_ms": 0,
      "end_ms": 4280,
      "start_frame": 0,
      "end_frame_exclusive": 103,
      "boundary_type": "hard_cut",
      "confidence": 0.94
    }
  ]
}
```

OmniShotCut 必须通过：

```text
models/omnishotcut/adapter.py
```

接入。

Celery Task 必须通过 Adapter 调用，禁止直接把第三方调用逻辑写入 API Route。

---

# 21. Scene 模型输入

Scene 模型读取：

```json
{
  "video_id": "video_001",
  "artifacts": {
    "shots": "storage://.../shots.json",
    "subtitles": "storage://.../subtitles.json",
    "visual_embeddings": "storage://.../visual_embeddings.npy",
    "audio_embeddings": "storage://.../audio_embeddings.npy"
  }
}
```

Scene 模型输出：

```json
{
  "video_id": "video_001",
  "boundaries": [
    {
      "after_shot_id": "shot_000010",
      "is_scene_boundary": true,
      "confidence": 0.89
    }
  ]
}
```

随后由独立步骤：

```text
merge_shots_to_scenes
```

生成 Scene。

不要让 Scene 模型内部同时负责：

- 推理；
- 数据库写入；
- 视频切片；
- API 返回；
- 最终 JSON 组装。

---

# 22. 数据库建议表

至少建立：

```text
projects
videos
tasks
model_runs
artifacts
shots
subtitle_segments
scenes
scene_evidence
```

必要字段：

## tasks

```text
task_id
video_id
task_type
status
stage
progress
created_at
started_at
finished_at
error_code
error_message
retry_count
celery_task_id
```

## model_runs

```text
run_id
task_id
video_id
model_name
model_version
weight_revision
code_revision
schema_version
parameters_json
status
device
runtime_ms
started_at
finished_at
```

## artifacts

```text
artifact_id
video_id
run_id
artifact_type
uri
format
schema_version
sha256
created_at
```

---

# 23. 下载 GitHub 模型前检查清单

每个模型下载前必须记录：

## 基础信息

- 仓库地址；
- 固定 Commit；
- 模型任务；
- 输入；
- 输出；
- 是否有预训练权重；
- 是否支持推理；
- 是否支持长视频；
- 是否支持批处理。

## 环境

- Python 版本；
- PyTorch 版本；
- CUDA 版本；
- FFmpeg 要求；
- 操作系统；
- GPU 显存要求。

## 输出定义

- 输出单位；
- 时间单位；
- End Frame 是否包含；
- Confidence 含义；
- 是否有转场范围；
- 是否需要后处理。

## 许可证

必须分别确认：

- 代码 License；
- 模型权重 License；
- 数据集 License；
- 是否允许商用；
- 是否要求署名；
- 是否允许再分发。

## 可维护性

- 最近 Commit；
- Issue 是否活跃；
- Release 是否存在；
- 安装问题是否普遍；
- 依赖是否锁定；
- 是否有测试。

## 性能测试

必须记录：

```text
1 分钟视频耗时
10 分钟视频耗时
GPU 显存占用
CPU 内存占用
输出大小
失败情况
```

---

# 24. MVP 实施步骤

必须按以下顺序实施。

## 阶段 1：基础工程

1. 创建 Git 仓库。
2. 建立项目目录。
3. 配置 `.gitignore`。
4. 建立 `.env.example`。
5. 建立 Docker Compose。
6. 启动：
   - FastAPI
   - Redis
   - Celery Worker
   - PostgreSQL
7. 建立健康检查接口。

完成标准：

```text
API 能访问
Redis 正常
Celery Worker 正常注册
数据库可连接
```

---

## 阶段 2：任务系统

1. 建立 Task 表。
2. 建立 Model Run 表。
3. 建立 Artifact 表。
4. 完成创建任务接口。
5. 完成任务查询接口。
6. 完成 Celery 测试任务。
7. 完成状态更新。
8. 完成失败记录。
9. 完成重试规则。

完成标准：

```text
API 创建任务
Celery 执行任务
数据库状态变化
前端可以查询
```

---

## 阶段 3：视频上传与标准化

1. 实现视频上传。
2. 生成：
   - project_id
   - video_id
   - task_id
3. 保存原始视频。
4. 使用 FFmpeg 生成：
   - normalized.mp4
   - audio.wav
   - metadata.json
5. 使用毫秒时间基。
6. 保存 Artifact。

完成标准：

```text
输入任意支持的视频
输出稳定的标准化视频与元数据
```

---

## 阶段 4：OmniShotCut

1. 检查仓库与 License。
2. 固定 Commit。
3. 安装依赖。
4. 编写独立测试脚本。
5. 编写 Adapter。
6. 编写 Celery Task。
7. 输出标准 `shots.json`。
8. 保存 Model Run。
9. 保存 Artifact。
10. 加入缓存。

完成标准：

```text
上传 MP4
异步执行
得到标准 shots.json
```

---

## 阶段 5：Whisper

1. 接入 Whisper Adapter。
2. 支持优先解析已有：
   - SRT
   - ASS
   - WebVTT
3. 无字幕时执行 Whisper。
4. 输出标准 `subtitles.json`。
5. 验证字幕与 Shot 时间对齐。

---

## 阶段 6：视觉与音频特征

1. 按 Shot 提取视觉特征。
2. 按 Shot 或时间窗提取音频特征。
3. 保存 `.npy` 或 `.npz`。
4. 生成 Feature Manifest。
5. 不把大型数组写入 Redis。
6. 不把大型数组直接写入 PostgreSQL。

---

## 阶段 7：Scene Boundary

1. 聚合：
   - Shot
   - Subtitle
   - Visual
   - Audio
   - Character
   - Location
2. 生成相邻 Shot 特征。
3. 预测 Scene Boundary。
4. 输出 `scene_boundaries.json`。
5. 合并 Shot。
6. 输出 `scenes.json`。

---

## 阶段 8：Scene Score

1. 只对 Scene 计算 `scene_score`。
2. 使用允许的 Scene Evidence。
3. 输出 `scene_scores.json`。
4. 禁止创建 action 或 plot 评分。
5. 组装 `final_result.json`。

---

## 阶段 9：稳定性

1. 加入结构化日志。
2. 加入缓存。
3. 加入幂等性。
4. 加入任务取消。
5. 加入 Worker 超时。
6. 加入磁盘空间检查。
7. 加入 GPU 显存错误处理。
8. 加入集成测试。

---

# 25. 测试规则

每个模型至少包含：

## 单元测试

测试：

- 输入校验；
- Schema 转换；
- 时间转换；
- 错误映射；
- Cache Key；
- 路径生成。

## 集成测试

测试：

```text
短视频
→ FastAPI
→ Celery
→ Redis
→ Model Adapter
→ Artifact
→ Database
→ API 查询结果
```

## 回归测试

必须保存一段固定测试视频与预期输出范围。

每次升级：

- 模型；
- 依赖；
- PyTorch；
- CUDA；
- FFmpeg；
- Schema；

都要重新运行回归测试。

---

# 26. 禁止事项

1. 禁止模型互相直接调用。
2. 禁止在 API 请求中执行长推理。
3. 禁止使用 FastAPI BackgroundTasks 替代 Celery 主流程。
4. 禁止将视频或大型 Tensor 放进 Redis。
5. 禁止把模型权重提交到 GitHub。
6. 禁止直接跟踪第三方仓库主分支。
7. 禁止写死本机绝对路径。
8. 禁止使用多套时间单位进行模型对齐。
9. 禁止只保存最终结果。
10. 禁止覆盖旧模型版本 Artifact。
11. 禁止每次任务重新下载权重。
12. 禁止每个请求重新加载模型。
13. 禁止把整部视频写入数据库。
14. 禁止无限自动重试。
15. 禁止未检查 License 就用于商业项目。
16. 禁止在系统中引入 action_score。
17. 禁止在系统中引入 plot_score。
18. 禁止模型内部自行操作 API、数据库和最终输出。
19. 禁止 Celery Task 直接返回大型数据。
20. 禁止没有 `task_id`、`video_id`、`run_id` 的日志。

---

# 27. Agent 执行原则

开发 Agent 必须遵循：

1. 先定义 Schema，再写模型调用。
2. 先完成 Celery 骨架，再接模型。
3. 先完成单模型闭环，再接第二个模型。
4. 每个模型必须独立 Adapter。
5. 每个模型必须独立 README。
6. 每个模型必须记录版本、权重与 License。
7. 每个 Celery Task 必须：
   - 可追踪；
   - 可重试；
   - 尽量幂等；
   - 保存 Artifact；
   - 更新状态；
   - 记录日志。
8. 大文件通过 URI 传递。
9. 小型结构数据通过 JSON 传递。
10. 所有跨模型时间对齐使用整数毫秒。
11. 所有结果必须可复现。
12. 所有失败必须可定位。
13. 所有中间步骤必须可单独重跑。
14. 已有成功 Artifact 必须允许复用。
15. 最终只输出 Scene 层结果和 Scene Score。

---

# 28. 最终架构原则

整个系统必须符合以下一句话：

> 原始视频只保存一次；所有长任务从 MVP 开始通过 Celery 执行；模型通过统一 Adapter 接入；模型之间不直接调用；大型数据通过 Artifact URI 传递；结构化信息通过统一 Schema 传递；编排器控制执行顺序；每一步保存可追踪、可复用、带版本的 Artifact；所有时间统一为整数毫秒；最终只在 Scene 层计算 Scene Score。


---

# 第二部分：算法与数据流程设计

# 影片自动分段算法与数据流程设计

- 保留文档 24 新增的字幕、音频、模型职责、Shot Boundary 聚类、时间调整和完整输出结构；保留文档 23 中的候选吸附、合并、多尺度重算、切分强度及伪代码规则。

# 1. 算法目标与总体原则
本系统根据用户选择的切分强度或目标平均片段长度，将完整影片自动切分为多个片段。系统采用“两大阶段、若干子步骤”的结构：

1.  候选召回阶段：生成 Shot Boundary，并仅使用场景模型生成 Scene Boundary 候选和场景语义标签。
2.  正式评分与选择阶段：将 Scene Boundary 候选匹配到 Shot Boundary 或保留为独立候选点，合并重复候选，围绕候选点读取场景上下文，正式计算 scene_score，再依据最短片段长度与目标数量选择最终切点。

模型不直接决定最终切点。最终切点由统一候选点结构、scene_score 和选择算法共同确定。

- 所有内部时间统一使用 timestamp_ms（整数毫秒）。
- 展示层可同时保留 timestamp（HH:MM:SS.mmm）。
- 所有强度、置信度、特征值和 scene_score 统一归一化到 [0,1]。
- 用户输入的特征权重范围为 0–10，系统在同一权重组内自动归一化。

# 2. 完整数据流
> 输入 MP4（可选字幕与音频辅助信息）
> ↓
> STEP 1A Shot Boundary 检测与多模型聚类
> STEP 1B 可选字幕解析或 Whisper 转写
> STEP 1C 可选环境声基础特征提取
> ↓
> STEP 2A Shot 抽帧
> STEP 2B SceneSeg / BaSSL 生成 Scene Boundary 候选
> STEP 2C 生成候选点前后的地点、人物组合、视觉语义、时间环境与环境声场景标签
> ↓
> STEP 3A Scene Boundary 候选匹配最近 Shot Boundary
> ├─ 距离 ≤ attachment_threshold：吸附
> └─ 距离 > attachment_threshold：保留为独立场景候选点
> ↓
> STEP 3B 合并重复或相近的场景候选点
> ↓
> STEP 3C 围绕每个候选点读取场景上下文并计算 scene_score
> ↓
> STEP 3D 清理影片首尾非法候选点，得到 valid_candidate_count
> ↓
> STEP 3E 按切分强度或目标平均片段长度计算 target_cut_count
> ↓
> STEP 3F 按 scene_score 从高到低选择非冲突候选点
> ↓
> 输出 final_cut_points 与 segments
> ↓
> FFmpeg 执行切分

# 3. STEP 1A：Shot Boundary 检测
## 3.1 输入与模型
输入格式：MP4。可启用以下模型：PySceneDetect、TransNet V2、Google Cloud Video Intelligence。

| **模型**                              | **适合承担**              | **不适合直接承担** |
|---------------------------------------|---------------------------|--------------------|
| PySceneDetect                         |                           |                    |
| TransNet V2                           | 逐帧 Shot Transition 概率 | 完整场景与叙事理解 |
| Google Cloud Video Intelligence       | 云端 Shot 列表与时间范围  | 深层语义边界判断   |
| **OmniShotCut** |                           |                    |

## 3.2 多模型结果聚类合并
三个模型分别输出原始边界。时间距离小于 shot_boundary_merge_tolerance 的边界聚为一组，每组生成一个统一 Shot Boundary。默认：

```text
shot_boundary_merge_tolerance_seconds = 0.20
```

统一时间建议使用组内时间戳的加权中位数。每个检测器的原始结果必须分别保存，并生成 detector_support_ratio：

```text
detector_support_ratio = 命中模型数 / 实际启用模型数
```

detector_support_ratio 仅用于 Shot Boundary 调试、质量分析和检测结果排序，不直接参与 scene_score。

## 3.3 输出结构
```json
{
"video_id": "movie_001",
"fps": 24.0,
"duration_ms": 7200000,
"shot_boundary_merge_tolerance_ms": 200,
"shots": [
{
"shot_id": "shot_0001",
"shot_start_ms": 0,
"shot_end_ms": 8230,
"duration_ms": 8230,
"start_frame": 0,
"end_frame": 197,
"start_timestamp": "00:00:00.000",
"end_timestamp": "00:00:08.230"
}
],
"shot_boundaries": [
{
"shot_boundary_id": "sb_0001",
"timestamp_ms": 8230,
"timestamp": "00:00:08.230",
"transition_type": "hard_cut",
"detectors": {
"pyscenedetect": {"detected": true, "raw_timestamp_ms": 8208},
"transnet_v2": {"detected": true, "raw_timestamp_ms": 8250, "frame_probability": 0.93},
"google_video_intelligence": {"detected": false, "raw_timestamp_ms": null}
},
"detector_support_ratio": 0.667
}
]
}
```

# 4. STEP 1B：字幕与语音准备
## 4.1 来源优先级
3.  SRT
4.  ASS
5.  WebVTT
6.  MP4 内嵌字幕轨
7.  MP4 音频经 Whisper 转写

## 4.2 Whisper 的职责边界
| **Whisper 负责**                                                    | **Whisper 不负责**                                                   |
|---------------------------------------------------------------------|----------------------------------------------------------------------|
| 语音识别、语言识别、带时间戳对白、无字幕时生成基础字幕数据          | 人物身份、对话意图、事件因果链、角色目标、准确音乐类别、正式切点决策 |
| **Qwen3-ASR-1.7B + Qwen3-ForcedAligner-0.6B** | 可识别中文                                                           |
| **SenseVoiceSmall**                           | 可识别音乐、背景音                                                   |
| **DSP**                                       | 可识别RMS、静音、突变、和长停顿                                      |

## 4.3 统一字幕 JSON
```json
{
"video_id": "movie_001",
"subtitle_source": "srt",
"language": "zh",
"subtitle_segments": [
{
"subtitle_id": "sub_0001",
"start_ms": 8120,
"end_ms": 11540,
"start_timestamp": "00:00:08.120",
"end_timestamp": "00:00:11.540",
"text_raw": "你为什么现在才告诉我？",
"text_clean": "你为什么现在才告诉我？",
"speaker_id": null,
"source": "srt",
"words": [{"word": "你", "start_ms": 8120, "end_ms": 8280}]
}
]
}
```

Whisper 时间戳可能存在长视频累计漂移、噪声误识别、多人重叠对白合并以及句子边界偏差。因此字幕时间戳用于候选证据生成、主题分析和上下文评分，不应被视为毫秒级最终切点。

# 5. STEP 1C：音频基础特征准备
在不新增模型名称的前提下，可通过数字信号处理从 MP4 音轨提取：

- RMS 能量
- 静音区间
- 音频突变
- 语音占比
- 非语音区间
- 长时间停顿
- 音乐或环境声变化线索

initial_strength 仅用于第一轮候选召回，不是正式评分。

# 6. STEP 2：Scene Boundary 候选与场景语义生成
## 6.1 目标
STEP 2 不直接输出最终切点，而是使用 SceneSeg、BaSSL 等场景模型生成 Scene Boundary 候选，并形成候选点前后的场景语义描述。系统只生成 Scene 类型标签。

- 场景候选依据开始时间
- 场景候选依据结束时间
- 推荐变化时间
- 场景标签类型
- 前状态
- 后状态
- 第一轮变化强度
- 模型置信度
- 来源模型

## 6.2 Shot 抽帧策略
- 每个 Shot 至少抽取开头 20%、中间 50%、结尾 80% 三帧。
- 较长 Shot 可增加采样帧。
- 较短 Shot 至少保留中间帧。

## 6.3 模型职责拆分
| **模型**       | **适合承担**                                            | **限制**                                 |
|----------------|---------------------------------------------------------|------------------------------------------|
| SceneSeg       | 连续 Shot 合并、Scene Boundary 判断、Shot-to-Scene 映射 | 不单独生成完整人物、地点、时间和叙事标签 |
| BaSSL          | 基于 Shot 序列上下文预测 Scene Boundary                 | 不承担精确人物身份、地点名称或剧情理解   |
| MovieNet       | 数据集、标签体系、训练与评估参考                        | 不是通用即插即用 MP4 推理模型            |
| MovieBench     |                                                         |                                          |
| Claude         | 结合字幕、关键帧和模型结果生成结构化语义描述            | 不负责毫秒级视觉边界检测                 |
| Qwen2.5-VL     | 候选视频窗口理解、视觉事件摘要                          | 不负责毫秒级最终切点                     |
| Whisper        | 对白时间轴                                              | 不负责人物意图与因果链                   |
| CRCSD          |                                                         |                                          |
| InternVideo2.5 |                                                         |                                          |
|                |                                                         |                                          |
|                |                                                         |                                          |

## 6.4 场景边界候选输出
```json
{
"scene_boundary_candidates": [
{
"label_id": "scene_boundary_001",
"label_group": "scene",
"label_type": "scene_boundary",
"recommended_timestamp_ms": 90500,
"source_shot_boundary_id": "sb_0015",
"before_shot_ids": ["shot_0011", "shot_0012", "shot_0013", "shot_0014"],
"after_shot_ids": ["shot_0015", "shot_0016", "shot_0017"],
"initial_strength": 0.82,
"model_confidence": 0.87,
"source_models": ["SceneSeg", "BaSSL"]
}
]
}
```

## 6.5 场景语义输出
```json
{
"scenes": [
{
"scene_id": "scene_0003",
"start_ms": 90500,
"end_ms": 152700,
"shot_ids": ["shot_0015", "shot_0016", "shot_0017"],
"characters": [
{"character_id": "unknown_character_01", "display_name": null, "presence_confidence": 0.91}
],
"location": {"label": "室内办公室", "confidence": 0.84},
"time_context": {"time_of_day": "night", "explicit_time_jump": false, "confidence": 0.72},
"scene_summary": "两名角色在办公室讨论失踪文件。",
"semantic_source": "Claude",
"visual_source": "VideoLLaMA2",
"scene_boundary_source": ["SceneSeg", "BaSSL"]
}
]
}
```

## 6.6 可生成的场景标签类型
| **场景标签组** | **示例**                                                         | **用途**                             |
|----------------|------------------------------------------------------------------|--------------------------------------|
| scene          | location_change、character_set_change、time_jump、scene_boundary | 候选召回与 scene_score 特征          |
| audio          | silence_start/end、ambience_change、music_change、speech_pause   | 候选召回、切点微调、scene_score 特征 |

# 7. STEP 3A：Scene Boundary 候选与 Shot Boundary 匹配
## 7.1 参数
> attachment_threshold_seconds = 2.5
> 产品建议输入范围：0–10 秒
> 算法层要求：attachment_threshold_seconds >= 0

产品层负责限制最大值；算法层读取配置并执行统一校验。

## 7.2 最近邻吸附规则
对每个 Scene Boundary 候选，找到距离推荐时间戳最近的 Shot Boundary：

```text
distance_ms = abs(recommended_timestamp_ms - nearest_shot_boundary_timestamp_ms)
```

- 若 distance_ms ≤ attachment_threshold_ms：吸附到该 Shot Boundary。
- 若 distance_ms > attachment_threshold_ms：保留为 non_shot_boundary 独立候选点。
- Scene Boundary 候选的原始时间戳必须保留，用于调试、误差分析、展示和模型迭代。

## 7.3 统一吸附策略
```text
attachment_strategy = "nearest"
# 可选扩展："nearest_with_previous_boundary_auxiliary"
# 辅助点只作为额外候选，不改变主吸附结果。
```

## 7.4 吸附输出
# 8. STEP 3B：候选点合并
## 8.1 合并条件
8.  多个 Scene Boundary 候选吸附到同一个 Shot Boundary。
9.  多个非 Shot 候选点的时间距离小于 non_shot_merge_tolerance。
10. 多个场景候选经判断指向同一次场景变化。

```text
non_shot_merge_tolerance_seconds = 1.0
```

## 8.2 合并后保留字段
- 候选点 ID、类型和时间戳
- 对应 Shot Boundary ID（如有）
- 全部原始 scene labels
- 原始推荐时间
- 来源模型
- 场景标签类型
- 第一轮变化强度
- 模型置信度

# 9. STEP 3C：正式计算 SceneScore
## 9.1 重新读取上下文
第一轮 Scene Boundary 预测只用于寻找候选区域。候选点生成后，系统必须重新读取候选点前后内容，正式计算 scene_score。不能只比较相邻两个 Shot，因为 Shot 时长差异较大，场景变化需要结合地点、人物组合、视觉语义、环境声和时间环境进行判断。

## 9.2 默认上下文窗口
```text
scene_context_seconds_each_side = 15
```

即默认读取候选点前 15 秒和后 15 秒。窗口可配置。

## 9.3 边界情况
- 候选点接近影片开头或结尾时，使用实际可用内容。
- 记录 effective_context_before_ms 与 effective_context_after_ms。
- 不补零、不复制帧。
- 可记录各场景特征的有效输入量与置信度，供调试与解释。

## 9.4 SceneScore 特征清单
为解决两文档中特征集合不一致，本统一版建立一个唯一默认特征清单。原文扩展项被纳入同一 SceneScore，而不是建立其他评分。

| **字段**                 | **含义**                    | **主要来源**                        | **默认输入强度** |
|--------------------------|-----------------------------|-------------------------------------|------------------|
| location_change          | 地点是否变化                | Scene 语义、Claude、VideoLLaMA 2    | 4                |
| character_set_change     | 人物组合是否变化            | 人物列表、跟踪结果、Claude          | 3                |
| visual_semantic_change   | 画面整体语义是否变化        | SceneSeg/BaSSL 上下文、VideoLLaMA 2 | 3                |
| time_jump                | 是否发生时间跳跃            | 字幕、场景摘要、Claude              | 2                |
| main_character_change    | 叙事中心人物是否变化        | 人物列表、Claude                    | 2                |
| shot_transition_strength | Shot 转场本身的视觉变化强度 | Shot 检测器结果                     | 1                |

支持全部 6 项。未启用或无法计算的特征应将对应权重设为 0，禁止用虚构值填充。

## 9.5 权重归一化与公式
```text
normalized_weight_i = raw_weight_i / sum(raw_weights)
scene_score = sum(normalized_weight_i * scene_feature_i)
```

- 每个 raw_weight_i 必须位于 0–10。
- 同一组权重之和必须大于 0。
- 每个 scene_feature_i 必须归一化到 [0,1]。
- scene_score 最终限制在 [0,1]。

## 9.6 默认配置与示例配置的区分
默认配置以本节表格为准。任何其他权重组合必须标记为 example，不得与 defaults 混用。

# 12. Minimum Segment Length
> minimum_segment_length_seconds > 12

- 12 秒无效；12.01 秒有效。
- 若界面只允许整数，最小值为 13 秒。
- 任何相邻最终切点之间的距离必须不小于最短片段长度。
- 影片开头到第一个切点、最后一个切点到影片结尾也必须满足该约束。

# 13. Target Cut Count
## 13.1 N 的统一统计时点
为消除两文档冲突，切分强度比例中的 N 统一定义为：候选合并与正式评分完成后，删除距离影片开头或结尾不足 Minimum Segment Length 的候选点后得到的 valid_candidate_count。候选点之间的最短间距冲突仍在后续选择阶段处理。

## 13.2 模式 A：切分强度档位
| **系统值** | **用户显示名称** | **比例** |
|------------|------------------|----------|
| high       | 精细切分         | 0.60     |
| medium     | 标准切分         | 0.30     |
| low        | 粗略切分         | 0.10     |

```text
target_cut_count = round(valid_candidate_count * intensity_ratio)
```

该数量是目标值，不是保证值。系统不得为了凑足数量而违反最短片段长度。

## 13.3 模式 B：目标平均片段长度
```text
target_segment_count = round(video_duration_seconds / target_average_segment_length_seconds)
target_cut_count = max(target_segment_count - 1, 0)
```

- target_average_segment_length_seconds 必须大于 minimum_segment_length_seconds。
- 建议明显大于最短片段长度。
- 理论最大切点数约为 floor(video_duration / minimum_segment_length) - 1。

# 14. 最终切点选择
## 14.1 MVP 贪心算法
11. 删除距离影片开头不足 Minimum Segment Length 的候选点。
12. 删除距离影片结尾不足 Minimum Segment Length 的候选点。
13. 按 scene_score 从高到低排序。
14. 选择当前最高分候选点。
15. 删除与已选点时间距离小于 Minimum Segment Length 的其他候选点。
16. 继续选择剩余候选点中的最高分点。
17. 达到 target_cut_count 或无可选点时停止。
18. 将最终切点按时间顺序排列。

## 14.2 局部最高分规则
在任一 Minimum Segment Length 冲突范围内，只保留 scene_score 最高的候选点。

| **时间** | **scene_score** | **结果** |
|----------|-----------------|----------|
| 60.2 秒  | 0.72            | 不保留   |
| 68.5 秒  | 0.84            | 保留     |
| 73.1 秒  | 0.75            | 不保留   |

## 14.3 候选不足
```json
{
"requested_target_cut_count": 30,
"actual_cut_count": 24,
"status": "insufficient_non_conflicting_candidates"
}
```

# 15. 统一输出 Schema
## 14.1 单个 Shot Boundary 候选点
## 14.2 独立非 Shot 候选点
## 14.3 STEP 3 完整输出
# 15. 完整默认配置
# 16. 核心伪代码
```python
def normalize_weights(weights: dict[str, float]) -> dict[str, float]:
for value in weights.values():
if value < 0 or value > 10:
raise ValueError("所有权重必须位于 0–10")
total = sum(weights.values())
if total <= 0:
raise ValueError("同一组权重不能全部为 0")
return {key: value / total for key, value in weights.items()}

def calculate_scene_score(
features: dict[str, float],
raw_weights: dict[str, float]
) -> float:
weights = normalize_weights(raw_weights)
missing = set(weights) - set(features)
if missing:
raise ValueError(f"缺少 SceneScore 特征: {sorted(missing)}")
for name, value in features.items():
if value < 0 or value > 1:
raise ValueError(f"特征 {name} 必须位于 [0,1]")
score = sum(weights[name] * features[name] for name in weights)
return min(max(score, 0.0), 1.0)

def calculate_target_cut_count(
valid_candidate_count: int,
mode: str,
intensity_level: str | None = None,
video_duration_seconds: float | None = None,
target_average_segment_length_seconds: float | None = None,
minimum_segment_length_seconds: float | None = None
) -> int:
if mode == "intensity_level":
ratios = {"high": 0.60, "medium": 0.30, "low": 0.10}
return round(valid_candidate_count * ratios[intensity_level])

if mode == "target_average_segment_length":
if target_average_segment_length_seconds <= minimum_segment_length_seconds:
raise ValueError("目标平均片段长度必须大于 Minimum Segment Length")
segment_count = round(
video_duration_seconds / target_average_segment_length_seconds
)
return max(segment_count - 1, 0)

raise ValueError("未知目标数量计算模式")

def select_final_cut_points(
candidates,
minimum_segment_length_seconds,
target_cut_count,
video_duration_seconds
):
if minimum_segment_length_seconds <= 12:
raise ValueError("Minimum Segment Length 必须大于 12 秒")

valid_candidates = [
candidate for candidate in candidates
if candidate.adjusted_cut_timestamp_seconds >= minimum_segment_length_seconds
and video_duration_seconds - candidate.adjusted_cut_timestamp_seconds
>= minimum_segment_length_seconds
]

ranked = sorted(
valid_candidates,
key=lambda item: item.scene_score,
reverse=True
)

selected = []
for candidate in ranked:
conflict = any(
abs(candidate.adjusted_cut_timestamp_seconds
- selected_candidate.adjusted_cut_timestamp_seconds)
< minimum_segment_length_seconds
for selected_candidate in selected
)
if conflict:
continue
selected.append(candidate)
if len(selected) >= target_cut_count:
break

return sorted(
selected,
key=lambda item: item.adjusted_cut_timestamp_seconds
)
```

# 17. 参数校验与异常处理
| **校验项**                            | **规则**                                            | **错误处理**             |
|---------------------------------------|-----------------------------------------------------|--------------------------|
| attachment_threshold_seconds          | 算法层 ≥ 0；产品建议 ≤ 10                           | 拒绝非法配置             |
| scene_internal_weights                | 每项 0–10，且总和 > 0                              | 要求恢复默认或重新输入   |
| scene_feature                         | 每项位于 [0,1]                                    | 拒绝计算并报告字段       |
| minimum_segment_length_seconds        | 必须 > 12                                          | 不得执行切分             |
| target_average_segment_length_seconds | 必须 > Minimum Segment Length                      | 提示修改                 |
| 候选数量不足                          | 允许 actual_cut_count < requested_target_cut_count | 返回不足状态，不强行补点 |

# 18. 冲突消解记录
| **原冲突**                      | **统一结果**                                                                         |
|---------------------------------|--------------------------------------------------------------------------------------|
| 正式评分范围不一致              | 只计算 scene_score；系统不建立其他评分或综合评分。                                   |
| 切分强度 N 统计时点不一致       | N = 删除影片首尾非法候选点后的 valid_candidate_count。                               |
| 叙事变化特殊吸附策略冲突        | 所有 Scene Boundary 候选统一使用 nearest 匹配。                                      |
| Attachment Threshold 范围不一致 | 算法层 ≥0；产品建议 0–10 秒。                                                        |
| SceneScore 特征集合不一致       | 建立唯一 11 项完整清单；基础版可启用前 6 项，其余权重设 0。                          |
| Shot Boundary 置信度结构不一致  | 采用 detectors + detector_support_ratio，不使用口径不明的单一 detection_confidence。 |
| 候选时间字段不一致              | 统一 original_timestamp_ms、adjusted_cut_timestamp_ms、cut_adjustment。              |
| 默认值与示例值混用              | 建立唯一 defaults；其他组合必须标记 example。                                        |
| 阶段编号不连续                  | 统一为两大阶段，内部使用 STEP 1A/1B/1C、STEP 2、STEP 3A–3F。                         |

# 19. 实施结论
- Shot 模型负责自然镜头边界。
- SceneSeg 与 BaSSL 负责 Shot 序列上的场景边界候选。
- MovieNet 作为数据集、标签体系与训练评估参考。
- Whisper 负责对白时间轴。
- Claude 与 VideoLLaMA 2 负责候选窗口的结构化语义理解。
- 字幕与音频仅作为 SceneScore 所需场景信息的辅助输入；系统不生成或其他变化类别。
- 统一评分模块仅计算 scene_score。
- 最终选择算法直接按 scene_score 排序，并满足最短片段长度和目标数量约束。
- FFmpeg 使用 adjusted_cut_timestamp_ms 执行实际切分。


---

# 第三部分：基础设施架构（2026-07-27 补全）

## 数据流架构

``text
API (FastAPI)
  → Redis (Broker)
    → Celery Queue Router
      ├── video      (CPU) → FFmpeg 预处理
      ├── shot       (GPU) → OmniShotCut
      ├── subtitle   (GPU) → Whisper
      ├── feature    (GPU) → Visual/Audio Encoder
      ├── scene      (GPU) → Scene Boundary + Score
      ├── final      (CPU) → Result Assembly
      └── maintenance(CPU) → Cleanup
  → Artifact Storage (Local/S3)
    → Database (PostgreSQL/SQLite)
``

## CPU/GPU Worker 拆分路径

``text
MVP (single worker):
  celery -A workers.celery_app worker -Q video,shot,subtitle,feature,scene,final,maintenance

Future (split):
  celery -A workers.celery_app worker -Q video,final,maintenance --concurrency=2 -n cpu@%h
  celery -A workers.celery_app worker -Q shot,subtitle,feature,scene --concurrency=1 -n gpu@%h
``

## 基础设施组件

| 组件 | 目录/文件 | 用途 |
|------|-----------|------|
| 数据库迁移 | lembic/ + lembic.ini | 版本化 schema 变更 |
| 分层依赖 | equirements/{base,api,worker,dev,models/} | 环境隔离 |
| 代码质量 | .pre-commit-config.yaml | Ruff + MyPy pre-commit |
| CI | .github/workflows/ci.yml | lint, type, test, migration, docker |
| 开发命令 | scripts/dev/{start,stop,test,lint,migrate,check}.py | 统一开发入口 |
| Artifact Manifest | core/artifacts/{manifest,writer,validator,hashing}.py | 文件级溯源 |
| 日志链路 | core/logging/{config,context,middleware}.py | request_id 贯穿 |
| 配置校验 | core/config.py | 启动时配置验证 |
| 健康检查 | /health/live + /health/ready | K8s 兼容探针 |
| 维护任务 | workers/tasks/maintenance_tasks.py | 临时文件清理 |
| 模型注册表 | models/registry.yaml | License + 来源追踪 |
| Worker 分组 | configs/workers.yaml | CPU/GPU 队列配置 |
