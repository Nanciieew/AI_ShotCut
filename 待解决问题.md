# 待解决问题

> 个人记录用，不代表项目 bug 或 IMPROVEMENTS。

---

## OmniShotCut

**【问题一】模型分段会多分（过分割）**

当前影响不大，后续 Scene 合并阶段会自然修复。frame-diff 已过滤掉最明显的误检（No_Cut_hard 3 FP），但 Hard_Cut_1 仍有轻微过分割（frame 661）。

**【问题二】淡入淡出 / dissolve 识别不出来**

OmniShotCut 在 128×96 分辨率下无法识别 dissolve 转场，全部标记为 General。Multiple_Cuts_smooth 只检出 3/7 shot。需要第二个模型或不同策略专门处理。

**【问题三】帧→ms 精度**

非整数 FPS（如 23.976）下是否会累积漂移？当前 30fps 测试正常。

**【问题四】GPU 性能**

GPU 推理速度对比 CPU 提升多少？当前无 GPU 可测。

**【问题五】default 模式的价值**

`default` 模式下的 inter_label 对 Scene Boundary 是否有参考？

---

## 项目基础

**【问题六】Docker 验证**

Docker Compose 尚未实际启动验证，需 Docker 环境。

**【问题七】Alembic 初始迁移**

初始 migration 文件尚未生成。

**【问题八】Celery Beat**

Maintenance tasks 的定时调度尚未接入。

---

## 后续模型

**【问题九】Whisper 接入**

字幕 / 语音识别的接入时机和方案。

**【问题十】Scene Boundary 选型**

Scene Boundary 候选模型（SceneSeg / BaSSL）的选型决策。

**【问题十三】FFmpeg scene filter 替代 OmniShotCut 的风险**

FFmpeg `select='gt(scene,0.1)'` 可实现纯 CPU 5 秒 shot 检测（vs OmniShotCut 35 分钟）。
但 Complete_test1 测试显示：
- 精度 82.3%，召回 86.7%
- 漏检 19 个 OmniShotCut 独有的 shot
- 其中 shot_000093 (9:48) 被 3/4 种评分模式选为场景边界 — FFmpeg 完全漏掉
- shot_000076 (7:44) 被 plot_only 模式漏掉
- shot_000119 (12:12) 被 location_only + custom 模式漏掉

**根因**：FFmpeg scene filter 纯像素差，检测不到暗切、淡入淡出、同色调场景切换。
OmniShotCut 的深度特征能捕获这些结构变化。

**当前策略**：保留 FFmpeg 作为无 GPU 环境的备选（`models/ffmpeg_scene/adapter.py`），
但生产环境仍用 OmniShotCut。VLM 打分不受影响 — 只是输入 shot 数量不同。

---

## VLM / 豆包 / 项目基础

**【问题十一】VLM (Qwen VL) 内容审核拦截**

调用 `qwen2.5-vl-72b-KFF1r3` 模型时返回：
```
ModelArts.81011: Input image May contain sensitive information, please try again.
```

**原因**：modelarts-maas 平台的图片内容审核将电影关键帧判定为敏感信息。

**尝试过的方案**：
- 缩小关键帧：320px → 160px — 无效，仍被拦截
- 文本消息调用：200 OK，模型和 Key 均正常

**解决方案**：联系 modelarts-maas 平台管理员，在控制台关闭或放宽 `qwen2.5-vl-72b-KFF1r3` 的图片内容审核策略。
管理后台：https://console.modelarts-maas.com → 模型部署 → 内容审核设置。

---

**【问题十二】Doubao ASR 公网 URL 依赖**

SeedASR API 要求 `audio.url` 为公网可访问的 HTTP URL。当前通过 ngrok 隧道暴露 localhost:8000，但 ngrok 重启后 URL 会变化，需手动更新 `.env` 中的 `PUBLIC_BASE_URL`。

**解决方案**：
1. ngrok 付费版可使用固定域名
2. 或上传音频到火山 TOS 对象存储，获得固定 URL
3. 或使用 Cloudflare Tunnel（免费固定域名）

---

**【问题十三】FFmpeg 8.1.1 libx264 1920×1080 编码崩溃**

`x264 [error]: malloc of size 4612516 failed` + `Cannot allocate memory`，即使系统有 5GB 空闲内存。

**已采用方案**：使用 320×180 proxy 替代全分辨率归一化（`build_shot_proxy_command`），8.6s 完成，无内存问题。

**根本解决**：降级 FFmpeg 到 7.x 或等待 8.1.2 修复。
