# 待解决问题

> 个人记录用，不代表项目 bug 或 IMPROVEMENTS。

---

## OmniShotCut

**【问题一】模型分段会多分（过分割）**

当前影响不大，后续 Scene 合并阶段会自然修复。frame-diff 已过滤掉最明显的误检（No_Cut_hard 3 FP），但 Hard_Cut_1 仍有轻微过分割（frame 661）。

**【问题二】淡入淡出 / dissolve 识别不出来**

OmniShotCut 在 128×96 分辨率下无法识别 dissolve 转场，全部标记为 General。Multiple_Cuts_smooth 只检出 3/7 shot。需要第二个模型或不同策略专门处理。

**【问题三】帧→ms 精度**

非整数 FPS（如 23.976）下是否会累积漂移？当前 30fps 测试正常。

**【问题四】GPU 性能**

GPU 推理速度对比 CPU 提升多少？当前无 GPU 可测。

**【问题五】default 模式的价值**

`default` 模式下的 inter_label 对 Scene Boundary 是否有参考？

---

## 项目基础

**【问题六】Docker 验证**

Docker Compose 尚未实际启动验证，需 Docker 环境。

**【问题七】Alembic 初始迁移**

初始 migration 文件尚未生成。

**【问题八】Celery Beat**

Maintenance tasks 的定时调度尚未接入。

---

## 后续模型

**【问题九】Whisper 接入**

字幕 / 语音识别的接入时机和方案。

**【问题十】Scene Boundary 选型**

Scene Boundary 候选模型（SceneSeg / BaSSL）的选型决策。

**【问题十三】FFmpeg scene filter 替代 OmniShotCut 的风险**

FFmpeg `select='gt(scene,0.1)'` 可实现纯 CPU 5 秒 shot 检测（vs OmniShotCut 35 分钟）。
但 Complete_test1 测试显示：
- 精度 82.3%，召回 86.7%
- 漏检 19 个 OmniShotCut 独有的 shot
- 其中 shot_000093 (9:48) 被 3/4 种评分模式选为场景边界 — FFmpeg 完全漏掉
- shot_000076 (7:44) 被 plot_only 模式漏掉
- shot_000119 (12:12) 被 location_only + custom 模式漏掉

**根因**：FFmpeg scene filter 纯像素差，检测不到暗切、淡入淡出、同色调场景切换。
OmniShotCut 的深度特征能捕获这些结构变化。

**当前策略**：保留 FFmpeg 作为无 GPU 环境的备选（`models/ffmpeg_scene/adapter.py`），
但生产环境仍用 OmniShotCut。VLM 打分不受影响 — 只是输入 shot 数量不同。
