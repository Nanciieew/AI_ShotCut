# OmniShotCut model dependencies
# Source: https://github.com/UVA-Computer-Vision-Lab/OmniShotCut @ 23ad6fb
# Verified from: pyproject.toml + requirements.txt + README.md
#
# Install:
#   pip install -r requirements/worker.txt
#   pip install -r requirements/models/omnishotcut.txt

# --- PyTorch CPU (no GPU available) ---
# Install first! Override to CPU-only:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision

# --- Core deps (pyproject.toml) ---
numpy>=1.26
ffmpeg-python==0.2.*
opencv-python-headless>=4.8
huggingface_hub>=0.25
Pillow>=10.0
packaging>=24.0

# --- OmniShotCut package (fixed commit) ---
# Installed via: pip install git+https://github.com/UVA-Computer-Vision-Lab/OmniShotCut.git@23ad6fb
# Listed here for documentation; actual install handled by Dockerfile
