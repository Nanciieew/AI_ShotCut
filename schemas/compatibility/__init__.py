"""Schema compatibility layer.

When schema versions change, add converters here to migrate
older artifacts to the current schema version.

Example:
  When v2 is introduced:
    compatibility/v1_to_v2.py  — converts v1 shots.json to v2 format
"""
