#!/usr/bin/env python3
"""
Model download script (placeholder).

When models are configured, this script will download weights
to model_store/ and record sha256 + metadata for each.

Usage:
    python scripts/download_models.py
"""


def main() -> None:
    print("Model download script — not yet implemented.")
    print("Configure models in configs/models.yaml, then add download logic here.")


if __name__ == "__main__":
    main()
